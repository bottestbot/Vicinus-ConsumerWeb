# Vicinus — Task Tracker
_Last updated: 2026-08-03 (SEO — organic + AI-answer-engine discoverability, SEO-01…14, from a full-app audit verified against prod: robots.txt/sitemap.xml both 404, `/neighbourhoods` has no server-rendered crawl path to its 41 detail pages, `/neighbourhoods/[slug]` ships an empty shell, zero JSON-LD sitewide; SEO-05 is a compliance decision gating all listing-indexing work; RENT — DDF residential rentals: rent lives in `TotalActualRent`, not `LeaseAmount`, RENT-01…05; MAPCOST — Mapbox/maps cost optimization from the 2026-07-26 cost audit, MAPCOST-01…06; VIBE — Neighbourhood Vibe data acquisition, VIBE-01…03, data-first only: OSM bike/green/noise geometry + StatCan Census 2021 demographics, Metro Van launch scope, scoring/UI downstream & out of scope; DASH — dashboard UI redesign to the new comp, DASH-01…10; Smart Search — natural-language → DDF filters, SMART-01…13, four-bucket parser, DDF-compliant by construction, reuses VERTEX-03; VERTEX — Gemini via Vertex AI on GCP, VERTEX-01…07; Vicinus IQ Brief — personalized dashboard brief, BRIEF-01…13, supersedes DATA-15/16/17 and closes JUL21FIX-10/11; July 21FIX — neighbourhood detail: listing 404 + price + POI tiles + homepage hero copy + search-label contrast + dashboard welcome copy + homepage copy sheet, JUL21FIX-01…14; CREA FIX tasks — DDF compliance hardening, CREA-01…08 incl. 07a-d; Neighbourhood Detail Page — design + livability, NBHD-01…17 complete)_

## Legend
- Severity: P0 (blocking) · P1 (significant) · P2 (polish)
- Status: [x] done · [~] partial · [ ] todo
- Sizes: S ≤0.5d · M 1–2d · L 3–5d · XL >5d (verification passes carry a QA size, not a build estimate)

## 🔍 SEO — organic + AI-answer-engine discoverability (2026-08-03)
_From a full-app SEO audit this session, verified against production. **The site is currently near-invisible to both Google and AI answer engines** — not for lack of content, but four structural gaps: (1) `vicinus.ca/robots.txt` and `vicinus.ca/sitemap.xml` both **404**, so Google has no URL inventory; (2) `/neighbourhoods` renders its grid client-side, so there is **no crawl path to any of the 41 neighbourhood pages** — combined with the missing sitemap they are undiscoverable; (3) `/neighbourhoods/[slug]` ships an **empty shell** — a live fetch of `/neighbourhoods/kitsilano` returns nav + CTA + footer only, no bio, no livability scores, no listings, because `NeighbourhoodDetailBody` is `'use client'` + TanStack Query; (4) **zero JSON-LD anywhere on the site**._

_**Why (2)+(3) hurt AI search worse than Google:** Google renders JS (with delay + deprioritization), but **GPTBot, ClaudeBot, PerplexityBot and ChatGPT-User do not execute JavaScript at all**. To them the neighbourhood pages are blank. Our most differentiated, most citable asset — deterministic livability scoring with a published methodology — is invisible to exactly the engines most likely to cite it._

_**Page inventory available today: ~112 publishable neighbourhood pages.** The DB holds ~416 rows (`bc-neighbourhoods.ts` seeds province-wide, far beyond the 41 in `metro-vancouver-neighbourhoods.ts`), and `getNeighbourhoods()` applies a `LAUNCH_CITIES` display filter (`src/lib/api/neighbourhoods.ts`) → **112 measured live against the prod API**. Earlier drafts said 41 then 31; both were estimates replayed against a single seed file rather than the database. The sitemap and the index grid both filter on `isLaunchCity` at runtime, so they agree by construction and neither needs a hardcoded count. Rows outside `LAUNCH_CITIES` sit on unverified coordinates and, per the filter's own comment, "would show collapsed or wrong scores" — so they are **excluded from the sitemap too**. Indexing known-wrong scores would contradict the methodology pages (SEO-08) shipping in the same phase, and the risk is asymmetric: bad pages take weeks to clear, while widening `LAUNCH_CITIES` widens the sitemap automatically. Plus `/buy` and `/rent` (which already carry canonicals — recent work, not yet on prod)._

_⚠️ **SEO-05 gates the listing-indexing work (SEO-11 counts, SEO-12).** Vicinus operates as a Real Estate Advertising Website on the DDF National Shared Pool, and CREA has historically restricted search-engine indexing of DDF listing detail. Neighbourhood, city and methodology pages are unambiguously ours and carry most of the ranking value — sequence those first regardless of how SEO-05 resolves._

- [ ] **SEO-01** [P0] _(Missing)_ **`robots.ts` + `sitemap.ts`** — both 404 on prod today. Add Next 16 metadata routes: `robots.ts` (allow all, declare the sitemap) and a **DB-driven** `sitemap.ts` enumerating static routes + all 41 neighbourhood slugs (never hardcode the list — it drifts). Set `lastModified` from the row's `updatedAt`. Hold the listing-URL portion until SEO-05 resolves. `S` · related: SEO-05, SEO-07 · _src/app/robots.ts (new), src/app/sitemap.ts (new), src/lib/api/neighbourhoods.ts_
- [ ] **SEO-02** [P0] _(Bug)_ **Server-render the neighbourhood index links** — `page.tsx` fetches all neighbourhoods server-side and then hands every card to `<NeighbourhoodsClient>`, so the rendered HTML contains **zero `<Link>`s to the 41 detail pages**. Verified live: the page returns nav + footer only. Render the full anchor grid in the server component and let the client component filter/reorder on top of it — the links must exist in HTML with or without JS. `S` · _src/app/(main)/neighbourhoods/page.tsx, src/components/neighbourhood/NeighbourhoodsClient.tsx_
- [ ] **SEO-03** [P0] _(Bug)_ **Server-render neighbourhood detail content** — the highest-SEO-value content on the site (bio, livability + sub-scores, median price, local essentials, live listings) is fetched client-side by `NeighbourhoodDetailBody` via `useNeighbourhoodDetail`, so no crawler without a JS engine sees any of it. Fetch the aggregate `/detail` payload in the server component and pass it down as initial data (keep the client component for interactivity). ⚠️ Keep `NeighbourhoodCTA` and the section spine order intact — this is a data-plumbing change, not a redesign. `M` · _src/app/(main)/neighbourhoods/[slug]/page.tsx, src/components/neighbourhood/NeighbourhoodDetailBody.tsx, src/hooks/useNeighbourhoodDetail.ts_
- [ ] **SEO-04** [P0] _(Missing)_ **JSON-LD — `Organization` · `WebSite` · `BreadcrumbList` · `RealEstateListing`** — there is no structured data anywhere on the site. Highest-leverage single addition for real estate, and it is how AI answer engines extract facts rather than skim prose. `Organization` + `WebSite` in the root layout; `BreadcrumbList` sitewide; `RealEstateListing` + `Residence` on property detail (price, beds, baths, sqft, geo, availability, brokerage). Property detail is **already a server component**, so this needs no rendering change. ⚠️ Brokerage/attribution fields must match what CREA-0x requires on-page. `M` · related: SEO-05 · _src/app/layout.tsx, src/app/(main)/properties/[id]/page.tsx, src/components/property/PropertyDetailView.tsx, src/lib/schema.ts (new)_
- [ ] **SEO-05** [P0] _(Decision)_ **DDF listing-indexing compliance check** — confirm what our DDF agreement permits before any listing URL enters the sitemap or gains an SEO slug. We are a REAW on the National Shared Pool, and CREA has historically restricted indexing of DDF listing detail. Output: a written yes/no/conditional that unblocks the listing half of SEO-01, the live counts in SEO-11, and SEO-12. **No code.** `S` · blocks: SEO-01 (listing portion), SEO-11, SEO-12 · _compliance + docs/_
- [ ] **SEO-06** [P1] _(Improvement)_ **Homepage: ISR + a keyword-bearing H1** — two problems in one file. (a) `export const dynamic = 'force-dynamic'` kills caching and TTFB on our most-crawled URL; featured listings do not need per-request freshness — move to `export const revalidate = 3600`. (b) The H1 is "Bringing You Closer to Home." — strong brand, **zero keyword value**: no "Canada", no "real estate", no city. Keep the line visually; make the H1 carry the terms, or add a server-rendered keyword section below the hero. ⚠️ Copy lives in `STRINGS`, not inline — change it there. `S` · _src/app/page.tsx, src/lib/strings.ts_
- [ ] **SEO-07** [P1] _(Missing)_ **`llms.txt` + explicit AI-crawler allowlist** — publish `/llms.txt` mapping our key pages and what data we hold. And when SEO-01 writes `robots.txt`, **explicitly allow `GPTBot`, `ClaudeBot`, `PerplexityBot`, `ChatGPT-User`, `Google-Extended`** — having no robots.txt is permissive today, but adding one that omits them is a silent block, which would be a self-inflicted wound given the whole point of SEO-03. `S` · deps: SEO-01 (same file) · _src/app/robots.ts, src/app/llms.txt/route.ts (new)_
- [ ] **SEO-08** [P1] _(Missing)_ **Publish the livability + walkability methodology as crawlable pages** — `docs/livability-methodology.md` and `docs/walkability-methodology.md` are original, defensible, citable methodology sitting in the repo where no crawler will ever see them. Original methodology is precisely what AI answer engines link to. Publish as real routes with proper headings and `TechArticle` JSON-LD, and link them from every neighbourhood page's livability panel. `M` · related: SEO-03 · _src/app/(main)/methodology/ (new), docs/livability-methodology.md, docs/walkability-methodology.md_
- [ ] **SEO-09** [P0] _(Missing)_ **JSON-LD — neighbourhood `Place` + `AggregateRating`** — emit `Place` (name, geo, containedInPlace → city/province) plus the livability composite as `AggregateRating`, and an `FAQPage` block for the common "what's it like to live in X" questions. This is the structured-data surface most likely to earn an AI citation. ⚠️ Hard dependency on SEO-03: there is nothing to emit until the data is server-side. `S` · deps: SEO-03 · related: SEO-04 (shares `src/lib/schema.ts`) · _src/app/(main)/neighbourhoods/[slug]/page.tsx, src/lib/schema.ts_
- [ ] **SEO-10** [P1] _(Bug-risk)_ **Canonical + `noindex` sweep** — only `/buy` and `/rent` have canonicals today. (a) Self-referencing canonicals on every indexable route. (b) `/buy` and `/rent` accept unbounded filter permutations → infinite duplicate URLs; `noindex` anything beyond a whitelisted param set. (c) Property detail is reachable both standalone and via the `@propertyModal` interception — canonical must point at the standalone URL. (d) `noindex` on `/dashboard`, `/feed`, `/onboarding`, `/sign-in`, `/sign-up` — thin/private pages burning crawl budget. ⚠️ Run **after** SEO-03/04/06 to avoid metadata-block merge conflicts. `M` · deps: SEO-03, SEO-06 · _src/app/(main)/buy/page.tsx, rent/page.tsx, properties/[id]/page.tsx, neighbourhoods/[slug]/page.tsx, (dashboard)/, (feed)/, (auth)/, onboarding/_
- [ ] **SEO-11** [P1] _(Missing)_ **City landing pages — `/buy/[city]` · `/rent/[city]`** — this is where the search volume actually lives ("homes for sale in Vancouver", "Burnaby condos for rent"), and today it is a **query param** (`/buy?city=Burnaby`), which rarely ranks. Add ~40 real server-rendered routes per listing type off the same 41-row neighbourhood/municipality set, each with unique intro copy, live counts, price stats and internal links to its neighbourhoods. Probably the single biggest traffic lever after the P0 work. ⚠️ Do **not** ship 80 near-identical thin pages — each needs genuinely distinct content or it is a duplicate-content penalty. ⚠️ Live counts depend on SEO-05. `L` · deps: SEO-01, SEO-04, SEO-05 · _src/app/(main)/buy/[city]/page.tsx (new), src/app/(main)/rent/[city]/page.tsx (new), src/lib/searchParams.ts, src/app/sitemap.ts_
- [ ] **SEO-12** [P2] _(Improvement)_ **Address slugs on property URLs** — `/properties/28374615` is opaque to users and rankers alike; `/properties/28374615/1234-w-broadway-vancouver-bc` reads better with the ID still authoritative. Must 301 the bare form and keep the `@propertyModal` interception working (it matches on the route shape). ⚠️ Gated on SEO-05 — pointless if listings can't be indexed. `M` · deps: SEO-05, SEO-10 · _src/app/(main)/properties/[id]/, src/app/(main)/@propertyModal/, next.config.ts_
- [ ] **SEO-13** [P1] _(QA)_ **Verification pass — crawl, render, rich results** — (a) confirm `robots.txt`/`sitemap.xml` resolve on prod and submit in Search Console; (b) **fetch each page type with JS disabled** and confirm neighbourhood bio/scores/links are present in raw HTML — this is the actual acceptance test for SEO-02/03; (c) validate every JSON-LD block in Google's Rich Results Test; (d) confirm no `noindex` leaked onto an indexable route. QA `M` · deps: SEO-01, SEO-02, SEO-03, SEO-04, SEO-09, SEO-10 · _manual + Search Console_
- [ ] **SEO-17** [P0] _(Data)_ **Neighbourhood bios do not exist — every row returns `bio: null`** — discovered while verifying SEO-03: **all ~416 neighbourhood rows have a null `bio`**, so `/detail` returns `description: ''` and the narrative section renders correctly with nothing to render. The `Neighbourhood.bio` column exists and the FE has always handled it; it was simply never populated. **This is the single biggest limit on the payoff of the whole SEO set.** SEO-02/03 make the pages crawlable, but what crawlers now find is livability scores and POI lists with no prose — the unique, citable narrative that actually differentiates a neighbourhood page from a data table is absent, and thin pages rank poorly however well-marked-up they are. Options: hand-written for the top N launch neighbourhoods, or LLM-drafted from the data we already hold (scores, POI mix, demographics) with human review — ⚠️ if generated, it must be reviewed, must not invent facts, and must not contradict the published methodology (SEO-08). **Content problem, not a code problem** — no code change ships this. `L` · related: SEO-03, SEO-08, SEO-13 · _content + api/prisma (data only)_
- [ ] **SEO-16** [P2] _(Data)_ **Backend: project `livabilityComputedAt` so the sitemap can emit `<lastmod>`** — the field exists on the Prisma model (`schema.prisma:241`) but `NeighbourhoodsService.listAll()`/`findBySlug()` use explicit `select` blocks that project neither it nor `createdAt`, so the frontend cannot supply a sitemap timestamp. `sitemap-data.ts` already reads `livabilityComputedAt ?? createdAt` and omits `lastModified` when both are absent — **deliberately not synthesised**, since a per-build timestamp trains crawlers to distrust `<lastmod>` entirely. One-line fix: add `livabilityComputedAt: true` to the `findMany` select and the matching field to `NeighbourhoodSummary`; nothing on the FE changes. ⚠️ The response is Redis-cached — the cache must expire before it surfaces. **Only backend task in the SEO set**; everything else is FE-only. `S` · deps: SEO-01 · _api/src/modules/neighbourhoods/neighbourhoods.service.ts:43-60_
- [ ] **SEO-15** [P1] _(Missing)_ **Neighbourhood comparison pages — `/neighbourhoods/[a]-vs-[b]`** — built entirely from livability sub-scores and POI counts we already hold, so **zero DDF dependency**. "X vs Y" queries are high-intent, low-competition, and AI answer engines strongly favour structured comparisons — the best volume-to-winnability ratio in the SEO set. ⚠️ **Do not generate all 820 pairs** from 41 neighbourhoods: pick the ~100 genuinely comparable ones (same city, adjacent, or similar tier) — thin permutations are a duplicate-content penalty, not a win. Reuses SEO-03's server-fetch pattern and SEO-04's `Place` builder. `M` · deps: SEO-03, SEO-04 · _src/app/(main)/neighbourhoods/compare/ (new), src/lib/schema.ts_
- [ ] **SEO-14** [P2] _(Missing)_ **Editorial layer — market reports + buyer guides** — there is no informational content on the site at all, so nothing can rank for the question-shaped queries that feed AI answer engines. Quarterly Metro Van market reports off our own DDF data + evergreen buyer guides, each linked into the relevant city/neighbourhood pages. Worth doing only once SEO-03/04/11 are live — otherwise it is content with no structural support. `L` · deps: SEO-03, SEO-11 · _src/app/(main)/guides/ (new)_

## 🏠 RENT — DDF residential rentals surfacing wrong (2026-07-27)
_From spot-check QA: [vicinus.ca/properties/30081177](https://vicinus.ca/properties/30081177) (517 Temiskaming Cr, Ottawa — a $2,650/mo rental on REALTOR.ca) shows **"Price on request"** and sits in the For Sale feed. Root cause verified live against DDF this session: **residential rentals store their rent in `TotalActualRent`, not `LeaseAmount`** — `LeaseAmount` (3,405 listings) is commercial-only; `TotalActualRent` (18,420 listings, incl. **13,589 Single Family + 1,107 Multi-family ≈ 14,700 residential rentals**; 732 in Ottawa, 216 in Vancouver) is where residential rents live, and the two populations are perfectly disjoint. Every rent-aware code path keyed off `LeaseAmount` alone, so: (1) all ~18K rentals pass the "For Sale" filter (`LeaseAmount eq null`) and pollute the default feed/search as sale listings with no price; (2) the "For Rent" tab returns only commercial leases; (3) rentals price as "Price on request" everywhere. **This also invalidates the 2026-06-17 "DDF has no residential rentals" conclusion** baked into the Smart Search plan (SMART bucket D "rental intent must hard-degrade") — rentals DO exist in the feed. No-price-anywhere listings = 0: every listing has a price in `ListPrice`, `LeaseAmount`, or `TotalActualRent`._

- [x] **RENT-01** [P0] _(Bug)_ **Backend: treat `TotalActualRent` as rent** — in `DdfQueryService`: For Rent filter → `(LeaseAmount ne null or TotalActualRent ne null)`, For Sale → both null; rent min/max price filters OR across both fields (OData null compares false, verified); `mapProperty` collapses `leaseAmount: LeaseAmount ?? TotalActualRent` so every consumer's `price ?? leaseAmount` fallback and For Rent labelling work; map-pins `$select` + price chain include `TotalActualRent`. **Verified against local API 2026-07-27:** `/search/listing/30081177` → `leaseAmount: 2650`; Ottawa For Rent = 867 residential listings with real rents; For Sale excludes them; 2000–3000/mo band = 541. `S` · _api/src/modules/ddf-sync/ddf-query.service.ts_
- [x] **RENT-02** [P1] _(Bug)_ **FE: rent price fallback + "/mo" suffix + Buy/Rent toggle** — `FeedView.mapListing` had NO `leaseAmount` fallback at all (feed rentals always "Price on request"); fixed. Threaded `leaseFrequency` through `Property`/`PropertyDetail` types, search/feed mappers, `SearchResultCard`/`MapListingPopup` → `PropertyCell` (suffix logic already existed, JUL21FIX-04), `PropertyStats` and `FeedCard` (both render "$2,650/mo"). Added **`ListingTypeToggle` (Buy | Rent pill) to the search filter bar** — writes `filters.listingType` (store already defaulted 'For Sale' with no UI; all queries already forwarded it) and clears the price band on switch; `PriceFilterPopover` is rent-aware ($0–10K / $100 steps, "Monthly rent" label). Also rent-aware detail metadata title (was "— $0.00M") and **Mortgage Analysis hidden on rentals** (was computing "$13/mo" off the rent). **Browser-verified 2026-07-27** on the Rent feed ($1,995/mo card), listing 30081177 ($2,650/mo + no mortgage panel), and a sale listing (unchanged: $379,900 + mortgage). `S` · _src/components/search/ListingTypeToggle.tsx (new), FilterPanel.tsx, PriceFilterPopover.tsx, FeedView.tsx, src/components/feed/FeedCard.tsx, src/components/property/PropertyStats.tsx, src/types/search.ts, src/types/property.ts, src/lib/api/properties.ts, src/app/(main)/search/SearchPageClient.tsx, src/app/(main)/properties/[id]/page.tsx_
- [x] **RENT-03** [P1] _(Bug-risk)_ **Audit remaining `leaseAmount` consumers + DB-backed paths** — root cause was the **write** side, not the readers: `ddf-property.sync.ts:mapProperty` mapped `LeaseAmount` alone, so every synced residential rental landed with `price` AND `leaseAmount` null and the neighbourhood page rendered "Price on request". Fixed to `LeaseAmount ?? TotalActualRent` (mirrors the RENT-01 live mapper); same one-liner in `seed-sample.ts`. The readers needed no change — `neighbourhoods.service.ts` already does `price ?? leaseAmount ?? 0`, and `saved-search-matcher.util.ts` reads the now-populated column. **Comps audit: clean** — every median/comp query (`getMarketSnapshot`, `computeBulkMedianPrices`, `properties.service.ts` city + price-band comps) filters `price: { gt: 0 }`, and rentals carry a null `price`, so no rent can enter a sale median. Backfill script added (`npm run backfill:rents`, `--dry-run` supported; DDF caps `$top` at 100) — **run locally 2026-07-29: 18,078 DDF rentals scanned, 17,495 rows filled, priceless actives 28,215 → 11,321** (the remainder genuinely have no amount in DDF). Browser-verified: City Centre Richmond live-listings carousel has zero "Price on request", rentals render "$5,200/mo". **Not yet run against prod.** `S` · deps: RENT-01 · _api/src/modules/ddf-sync/ddf-property.sync.ts, api/src/scripts/backfill-rental-amounts.ts (new), api/src/scripts/seed-sample.ts_
- [x] **RENT-04** [P0] _(QA)_ **Verify + deploy** — local verification 2026-07-27 (API + browser pass, see RENT-01/02); shipped in `1268039`, **prod re-verified 2026-07-27**: prod API Ottawa For Rent = 865 with real rents / For Sale clean; [vicinus.ca/properties/30081177](https://vicinus.ca/properties/30081177) renders "$2,650/mo" (title + stats card) with no mortgage panel; Buy|Rent toggle live on vicinus.ca/search. QA `S` · deps: RENT-01, RENT-02 · _manual + preview_
- [ ] **RENT-05** [P2] _(Decision)_ **Product surface for rentals** — ~14.7K residential rentals just became reachable: decide how the For Rent experience surfaces (feed mixing policy, rental filters like pet-friendly/furnished already stubbed in `SearchFiltersExtended`, rent-band UI vs sale price slider), and **correct the Smart Search plan's bucket-D assumption** (SMART section: "no residential rentals in the feed" is false — rental intent should map to the For Rent filter, not hard-degrade). `M` · deps: RENT-04 · _product + TASKS.md SMART section_

## 💸 MAPCOST — Mapbox/maps cost optimization (2026-07-26)
_From the 2026-07-26 maps-cost audit. **Google runtime cost is $0 by design** (search autocomplete is our own DB endpoint; the Google tile proxy is a Mapbox-token-missing fallback only; `getNearbyPlaces()` is dead code) — all recurring spend is Mapbox. The dominant driver: **every property-detail view mounts 2 Mapbox GL maps** (`ActivityMapInner` + `NeighbourhoodMiniMap`), billed per load (~$5/1K after 50K free/mo) even when the user never scrolls to them — `next/dynamic` splits the bundle but does not defer mounting. Est. ~$9.5–12K/mo at 150K MAU as implemented; ~$3.5–6K after these fixes. Static Images (~$1/1K after 50K) and Geocoding (~$0.75/1K after 100K) are the secondary drivers._

- [ ] **MAPCOST-01** [P1] _(Efficiency)_ **Lazy-mount the two property-detail GL maps** — gate `ListingActivityMap` and `NeighbourhoodContextScore`'s mini-map behind an `IntersectionObserver` (mount on scroll-into-view; static-image or skeleton placeholder until then), and add `reuseMaps` to both (the search `MapView` already has it). Consider merging the two into one map with layered markers — they render the same area. This alone cuts total map loads ~60%. `M` · _src/components/property/ListingActivityMap.tsx, src/components/property/NeighbourhoodContextScore.tsx, src/components/property/ActivityMapInner.tsx, src/components/property/NeighbourhoodMiniMap.tsx_
- [ ] **MAPCOST-02** [P1] _(Efficiency)_ **Stop per-user Static Images fan-out on neighbourhood cards** — `NeighbourhoodsClient` builds one `api.mapbox.com` static-image URL per card and every user's browser fetches them directly (no CDN), so identical images bill once per viewer; home page adds 4 more (featured cities) and the detail page 3 (hero + 2 LocalInfo tiles). Fix: serve card images from the already-seeded neighbourhood photos in the DB where present, and/or front the static URLs with Netlify's image CDN — ⚠️ check Mapbox ToS caching limits before proxying. `M` · _src/components/neighbourhood/NeighbourhoodsClient.tsx:101, src/app/page.tsx:153, src/lib/neighbourhood-images.ts, src/components/neighbourhood/LocalInfoTiles.tsx_
- [ ] **MAPCOST-03** [P1] _(Efficiency)_ **Delete redundant client-side geocoding on search select** — `SearchBar` calls Mapbox `geocodeCity()` on every suggestion select AND every Enter, but the suggestion came from our own DB which already stores coordinates. Return `lat/lng` in the `/search/autocomplete` payload (BE: `search.service.ts`; type: `AutocompleteSuggestion`) and set `setGeocodedCenter` from it directly — removes both calls and the fly-to gets *faster* (no network round-trip). `S` · _src/components/search/SearchBar.tsx:87,109, api/src/modules/search/search.service.ts, src/types/search.ts_
- [ ] **MAPCOST-04** [P2] _(Efficiency)_ **Widen the sell-page address-autocomplete debounce** — Mapbox Geocoding has no session concept; every request bills. `SellIntro` fires at 180ms pauses; widen to ~350ms and require ≥3 chars before querying (~2× fewer requests, no perceptible UX cost on an address field). `S` · _src/components/sell/SellIntro.tsx, src/lib/geocode.ts_
- [ ] **MAPCOST-05** [P2] _(Chore)_ **Remove dead Google code + guard the $0 posture** — delete `getNearbyPlaces()` (zero callers, and it targets the *legacy* `nearbysearch` endpoint) and the `maps.googleapis.com` remotePattern in `next.config.ts` (confirm nothing renders Google image URLs first). Then make the $0 stick: set a billing cap/alert on the Google Cloud key (Places + Static Maps + Street View are all enabled on it), and alert if `NEXT_PUBLIC_MAPBOX_TOKEN` is missing at build — a dropped token silently flips neighbourhood tiles onto the billed Google fallback. `S` · _api/src/modules/neighbourhoods/google-maps-proxy.service.ts:102-139, next.config.ts:35, netlify.toml_
- [ ] **MAPCOST-06** [P2] _(Data)_ **Instrument real usage for the cost model** — the audit's dollar figures rest on assumed traffic; capture the actual `[MEASURE]` inputs in PostHog: map mounts per session (fire an event on each GL map load, tagged by surface), property-detail views/session, static-image surfaces viewed/session, geocode calls/session, and Sx (search sessions/user). Re-run the cost formulas with 30 days of real data and verify plan rates against the Mapbox invoice. `S` · related: DATA-01 (PostHog) · _FE analytics + docs/_

## 🌆 VIBE — Neighbourhood Vibe: data acquisition (2026-07-22)
_A "neighbourhood vibe" feature — a richer characterisation of an area (bike-friendly · green · quiet · lively · family vs. young-professional) layered on top of the four existing livability sub-scores (Walk 0.30 · Schools 0.25 · Amenities 0.25 · Transit 0.20, `scoring/blend.ts`). **This section is DATA-FIRST only** — acquire + store the raw inputs. Scoring, the vibe model, and any UI are downstream and NOT scoped here._
_**Launch scope = the same 10 Metro Van cities** the transit scope covers (`livability.service.ts` `TRANSIT_SCOPE_CITIES`) — don't acquire province-wide._

_**What we already have (verified this session — no new fetch needed):** `poi-ingestion.service.ts` runs one Overpass `around:1500,<centroid>` query per neighbourhood and stores point POIs in `NeighbourhoodPoi` (snapshotted `YYYY-Qn`, `category` ∈ the fixed `PoiCategory` enum in `scoring/geo.ts`, no PostGIS). It already ingests **restaurant · cafe · bar** (→ dining/nightlife vibrancy is computable today) and **supermarket** (→ grocery). Parks are ingested but only as **points**, not area._

_**The one architectural catch that governs VIBE-01:** bike lanes, green cover, and roads are OSM **ways/relations (lines & polygons), not point nodes** — they do **not** fit `NeighbourhoodPoi`'s point shape or the `PoiCategory` enum. Decide storage before building: (a) precompute per-neighbourhood **metrics** (e.g. `bikeLaneKm`, `greenCoverPct`, `highwayProximityM`) onto new `Neighbourhood` columns, vs. (b) a new geometry table. Recommendation: (a) — the vibe layer needs summary metrics per area, not queryable geometry, and it avoids the PostGIS gap SMART-06 is still deciding._

- [~] **VIBE-01** [P1] _(Data)_ **Extend OSM/Overpass ingestion for bike · green-cover · noise geometry** — add to the existing Overpass query in `poi-ingestion.service.ts`: bike infra (`highway=cycleway`, `cycleway=*` on roads, `route=bicycle`), green-cover polygons (`landuse=grass|forest|recreation_ground|meadow`, `leisure=garden|nature_reserve|pitch`), and noise/traffic geometry (`highway=motorway|trunk|primary`, `railway=rail`, `aeroway`). Reduce each to per-neighbourhood **metrics** (bike-lane length in km within radius, green-cover % of area, metres to nearest major road / rail / airport) and persist to new `Neighbourhood` columns — **not** `NeighbourhoodPoi` (see the catch above). Keep the quarterly snapshot discipline. `L` · _api/src/modules/neighbourhoods/poi-ingestion.service.ts, api/prisma/schema.prisma_
  - **Built (2026-07-22, tsc-clean, metric math unit-sanity-checked):** shared `overpass.ts` client (extracted endpoint/retry/UA from `poi-ingestion` so both callers share it); new `VibeMetricsService` (`vibe-metrics.service.ts`) runs a separate `out geom` query and reduces ways→metrics; line/polygon primitives added to `scoring/geo.ts` (`polylineMeters`/`polygonAreaSqMeters`/`nearestVertexMeters`, local equirectangular projection); 7 new `Neighbourhood` columns + migration `20260722230000_vibe_metrics_osm_inputs` (hand-authored, **not yet applied to the DB**); batch script `scripts/ingest-vibe-metrics.ts` + `npm run vibe:neighbourhoods` (`--only-missing`). **Chose per-neighbourhood metrics on `Neighbourhood`, not a geometry table** (option (a)).
  - **Remaining:** apply the migration (`prisma migrate deploy`) and run `npm run vibe:neighbourhoods` against the launch cities to populate + eyeball real values (green% / bike-km sanity per known area). Relations (`route=bicycle`) are **not** summed yet — only ways; note if bike-km looks low. Then `[x]`.
- [ ] **VIBE-02** [P1] _(Data)_ **StatCan Census 2021 demographics ingest** — pull the free 2021 Census profile at **dissemination-area** level (age mix, family/household composition, median income band, owner-vs-renter, dwelling type) and aggregate DAs to each neighbourhood via the existing `boundary` GeoJSON polygon (point-in-polygon on DA centroids; fall back to `centroidLat/Lng` where a boundary is absent). Store as a structured `demographics Json?` panel on `Neighbourhood`, not scored. New source — write a standalone ingest script like `scripts/download-translink-gtfs.ts`. **Surface StatCan attribution** wherever displayed. `L` · _api/src/scripts/ (new), api/prisma/schema.prisma_
- [ ] **VIBE-03** [P2] _(Chore)_ **Vibe input audit + gap note** — after 01–02 land, one-off pass documenting, per launch city, which vibe inputs are populated vs. null (OSM density, census DA coverage) so the downstream vibe model knows what it can honestly rely on and what must degrade. `S` · deps: VIBE-01, VIBE-02 · _api/src/scripts/_

## 🖥️ DASH — dashboard UI redesign to the new comp (2026-07-22)
_From a supplied target screenshot of the signed-in dashboard. Restructures the page into a **left content column + full-height Notifications sidebar** and adds several net-new pieces. Current layout (verified this session, `src/app/(dashboard)/dashboard/page.tsx`): `WelcomeBanner` (h1 + brief + search + recent) → a 2/3 · 1/3 grid of `FeaturedProperty`+`SavedProperties`+`OpenHouseSchedule` / `NotificationsPanel` → `VisitedProperties` full-width. Tasks only — no implementation._
_⚠️ **The screenshot's data is mock/seed** ("651 Nootka · Saturday, Oct 12", "95 unread", "9 new matches today"). Build every count/label against **real** data, never the literal values — see DASH-02._
_**Sequencing:** DASH-03 (brief redesign) is downstream of the pending IQ-Brief **7-day-window + salience** decision and a `BriefHighlight` contract change — don't restyle the brief until those land or it's rework. DASH-06 (search toggle) is **already owned by SMART-09/11** — this section only tracks slotting it into the new layout._

- [ ] **DASH-01** [P1] _(Improvement)_ **Two-column shell + section reflow** — restructure `page.tsx` to the comp's order inside a left column (IQ Brief → search block → Next Open House hero → Saved → Open-house schedule → Recently viewed) with `NotificationsPanel` as a right sidebar that runs the full column height. Moves the search bar **out of** `WelcomeBanner` into its own block and drops/repurposes the current `FeaturedProperty` slot (see DASH-05). `M` · _src/app/(dashboard)/dashboard/page.tsx_
- [ ] **DASH-02** [P1] _(Bug-risk)_ **Welcome summary metric bar — counts MUST be real** — the comp's subline reads "**6 properties** · 3 open houses this week · 9 new matches today". This is the exact shape of the fabricated count we just deleted (JUL21FIX-11) — a "9 new matches today" that counts nothing is the same trust/DDF problem. `saved.length` is real; **"open houses this week"** must come from `OpenHouse`/`OpenHouseVisit` in the week window and **"new matches today"** from `NEW_LISTING` alerts created today. Decide each source before building; if one can't be computed honestly, drop that clause rather than fake it. `S` · _src/components/dashboard/WelcomeBanner.tsx_
- [ ] **DASH-03** [P1] _(Improvement)_ **IQ Brief redesign → "Your week ahead"** — restyle `VicinusBrief` to the comp: eyebrow, "Your week ahead" title, one-line body, a **2×2 grid of larger highlight tiles** (each: address, time range, and a "• match" dot when `isMatch`), an **"Add all to calendar"** header action (DASH-04), and a "View all on map" tile. ⚠️ The `BriefHighlight` contract has **no time-range field** today and match state isn't surfaced per tile — extend the contract (API + `src/types/dashboard.ts`) first. ⚠️ **Blocked on** the pending 7-day-window + salience decision — that governs which highlights appear here. `L` · deps: IQ-Brief window/salience decision · _src/components/dashboard/VicinusBrief.tsx, api/src/modules/brief/_
- [ ] **DASH-04** [P2] _(Missing)_ **Calendar actions — "Add all to calendar" + per-card "Add to calendar"** — new capability; there is **no ICS/calendar util** in the repo today. Generate an `.ics` (or Google Calendar link) for a single open house and for the week's set. Decide ICS-download vs. Google-link before building. `M` · related: DASH-03, DASH-05 · _src/lib/ (new), src/components/dashboard/_
- [ ] **DASH-05** [P1] _(Missing)_ **Next Open House hero card** — new component: large image card with a "NEXT OPEN HOUSE" badge, address, date/time, and "Add to calendar", sourced from the user's **next upcoming** open house (`OpenHouseSchedule`/`OpenHouseVisit`, else nearest). Decide whether it **replaces** the current `FeaturedProperty` (saved/visited) slot or sits alongside — the comp shows it where Featured is today. `M` · _src/components/dashboard/ (new), src/app/(dashboard)/dashboard/page.tsx_
- [ ] **DASH-06** [P2] _(Improvement)_ **Slot the Smart/Classic search toggle into the new layout** — the comp's "Smart `BETA` / Classic" toggle + helper copy is **already specced in SMART-09/11**; this task is only the dashboard-placement/styling of that component in the reflowed left column, not a second implementation. Do **not** ship a toggle with no Smart behind it — gate on SMART-09. `S` · deps: SMART-09 · related: SMART-11 · _src/components/dashboard/_
- [ ] **DASH-07** [P2] _(Improvement)_ **Saved-property cards → "Schedule tour" CTA** — the comp adds a per-card "Schedule tour" button. Decide its target (contact-agent flow vs. a booking stub) before wiring — don't ship a button that no-ops. `S` · _src/components/dashboard/SavedProperties.tsx_
- [ ] **DASH-08** [P2] _(Improvement)_ **Open-house schedule → status pills** — the comp shows **Attended / Skipped** as pills; the current `OpenHouseSchedule` uses a `<select>` dropdown. Restyle to pills, keep the existing PATCH/`useOpenHouseVisits` behavior. `S` · _src/components/dashboard/OpenHouseSchedule.tsx_
- [ ] **DASH-09** [P2] _(Improvement)_ **Notifications panel polish** — layer the comp's refinements onto the existing panel (which already has All/Alerts/Schedule tabs + type stripes): per-item **NEW** badge, price-drop row highlighted with a pink/red left stripe, **pagination (1…10)**, an "N unread" count, and "Mark all read". ⚠️ "Mark all read" **hard-deletes** alerts (`alerts.service.ts:128`), and both this panel and the IQ Brief read the same `Alert` rows — confirm marking-read here doesn't empty the brief. `M` · _src/components/dashboard/NotificationsPanel.tsx_
- [ ] **DASH-10** [P2] _(Improvement)_ **Visual polish pass** — spacing, card radii, typography, and section dividers across all dashboard sections to match the comp once the structural tasks land. QA-style, do last. `S` · _src/components/dashboard/*_

## 🔎 Smart Search — natural-language → DDF filters (2026-07-22)
_A single plain-language field on the dashboard ("house with a pool near a park and school, under $1.4M") that resolves to editable filter chips and runs the existing DDF search. Ships behind a Smart/Classic toggle (the toggle is the feature flag's UI face)._

_**Architecture — the one rule that governs every task below: the LLM parses the sentence, it never touches a listing.** The model sees only the user's text and emits a validated JSON filter object; DDF is then queried deterministically by the existing `SearchService`. Nothing from CREA enters a prompt, nothing is LLM-generated about a listing, so this is **DDF-compliant by construction** — the same posture as the IQ Brief, inverted (there the LLM writes prose over facts; here it reads text and emits filters)._

_**The four buckets every parsed token sorts into** — the whole plan is built around them: **A Native** (maps straight to `SearchQueryDto` + a `Property` column — ships day 1, no new infra), **B Amenity** (true of the listing but buried in `Property.exteriorFeatures` JSON — needs SMART-05), **C Proximity** ("near a park / walk to a school" — the differentiator, backed by the owned `NeighbourhoodPoi`/OSM layer, needs SMART-06/07), **D Degrade** (can't honestly answer — rentals, "quiet street", "good schools" — refused in plain language, never faked)._

_**Schema truths this is built on (verified 2026-07-22):** (1) there is **no `StructureType` column** — `Property.propertySubType` is the only stored dwelling-form field despite the DTO exposing `structureType`, so house/condo/townhouse needs an explicit normalization map. (2) `exteriorFeatures` is `Json?` — not queryable for `pool`/`garage` without SMART-05. (3) `NeighbourhoodPoi` stores `lat`/`lng` floats scoped **by neighbourhood** with no PostGIS + no spatial index, so arbitrary point-radius ("within 800m of this listing") needs the POIs promoted to a listing-queryable spatial layer. (4) per the DDF/REAW tier there are **no residential rentals** in the feed — rental intent must hard-degrade._

_**Reuse the VERTEX shared Gemini provider** (VERTEX-03) rather than a fourth call site; if VERTEX hasn't landed, write the parser against `@google/genai` so it ports for free._

### Parse contract + parser
- [ ] **SMART-01** [P1] Define the parse-output contract — extend `SearchQueryDto` into a `ParsedQuery` shape covering all four buckets: native fields (city, province, minPrice/maxPrice, beds, baths, propertySubType[], minSqft/maxSqft, yearBuiltMin, parkingMin, listingType, status), **new** `minLotSize` (column `lotSize` already exists), `features[]` (bucket B), `near[] {poi, radiusM}` (bucket C), and `rejected[] {phrase, reason}` (bucket D, surfaced to the user). Ship the dwelling-form normalization map here (house/detached→`propertySubType` values actually present in the data — audit `distinct propertySubType` first). `M` · _api/src/modules/search/dto/_
- [ ] **SMART-02** [P1] LLM parser service — sentence→`ParsedQuery` via Gemini structured output (`responseSchema`, temperature 0), **prompt sees only the sentence**, never a listing. Cache on the **normalized sentence** (lowercased, trimmed) so a repeated query costs ~0 and is deterministic. Prompt rule stated explicitly: *only emit fields you're confident about; put anything you can't map into `rejected[]` with a reason — never invent a filter to seem helpful.* `M` · deps: SMART-01, VERTEX-03 (or `@google/genai`) · _api/src/modules/search/_
- [ ] **SMART-03** [P1] Validation + confidence guardrail — every parser output runs back through `class-validator` before it can reach SQL; unknown/out-of-range fields are dropped, not executed (no injection surface). Emit a per-query confidence; below threshold, **fall back to free-text `q`** and tell the user rather than returning a confidently-wrong set. Rental intent (`listingType: rent`) short-circuits to the honest "no rentals yet" response. `S` · deps: SMART-02 · _api/src/modules/search/_

### Data layer (buckets B + C)
- [ ] **SMART-04** [P2] Audit + normalization map for `propertySubType` — one-off analytical pass over live data to enumerate the actual values, so SMART-01's map is grounded in what DDF really sends (not assumed labels). Feeds SMART-01. `S` · _api/src/scripts/_
- [ ] **SMART-05** [P1] Amenity normalization at sync time (**bucket B**) — parse `exteriorFeatures` (+ `parkingFeatures`, `cooling`) into a queryable `String[]` enum column (`amenities`) on `Property`, populated in `ddf-property.sync.ts`; migration + re-runnable backfill over existing rows. Start with a small controlled vocab (pool, garage, waterfront, fireplace, a/c) — this unblocks far more than smart search. `M` · _api/prisma/schema.prisma, api/src/modules/ddf-sync/ddf-property.sync.ts, api/src/scripts/_
- [ ] **SMART-06** [P1] Promote POIs to a listing-queryable spatial layer (**bucket C, the moat**) — `NeighbourhoodPoi` can't answer point-radius. Add a PostGIS `geometry(Point)` (or a bbox-prefiltered haversine if avoiding the PostGIS dep) + spatial index, decoupled from `neighbourhoodId` so any listing point can query it. Decision gate: PostGIS extension vs. app-side haversine — pick before building. `L` · _api/prisma/schema.prisma, api/src/modules/neighbourhoods/_
- [ ] **SMART-07** [P1] Proximity filter in the query path — given a `ParsedQuery.near[]`, filter listings whose point has ≥1 POI of each requested category within `radiusM`. Compose with the existing `SearchService` WHERE-building so native + amenity + proximity are one query, one page of results. `M` · deps: SMART-06, SMART-05 · _api/src/modules/search/_

### API + frontend
- [ ] **SMART-08** [P1] `POST /search/parse` (or extend the search endpoint) → `{ filters, chips[], rejected[], confidence, resultCount }`. `chips[]` is the human-readable projection of `filters` (label + removable), computed server-side so FE and share-links agree. `S` · deps: SMART-03, SMART-07 · _api/src/modules/search/_
- [ ] **SMART-09** [P1] `<SmartSearch />` — the white-card component from the design: NL field, spark icon, **editable chips** (each removable → re-runs search), `rejected[]` rendered honestly ("couldn't filter X"), low-confidence free-text fallback banner, shared Recent searches. Behind a Smart/Classic toggle that swaps the existing `HeroSearchBar` in place. `L` · deps: SMART-08 · _src/components/dashboard/ (new), src/components/landing/HeroSearchBar.tsx_
- [ ] **SMART-10** [P2] Shareable search URL — encode `filters` in the querystring so a parsed search is a linkable, screenshotable artifact ("6 homes near a park + school under $1.4M"); server-rendered OG image for the viral loop. Result viewable **before** any signup wall; gate save/alerts, not viewing. `M` · deps: SMART-08 · _src/app/(main)/search/, OG route_
- [ ] **SMART-11** [P2] Toggle persistence + feature flag — Smart is default (with a `BETA` tag), Classic always one click away; persist a user's manual choice (localStorage → profile). The flag gates Smart for a rollout cohort; **a manual switch back to Classic is the key failure signal** to log. `S` · deps: SMART-09 · _FE + flag_
- [ ] **SMART-12** [P2] Telemetry — log parse confidence, which chips users delete (parser weakness), `rejected[]` frequency by phrase (roadmap for buckets B/C/D), and Smart→Classic switch-backs. No listing data in events. `S` · deps: SMART-09 · related: DATA-01 (PostHog) · _FE + api_

### Verification
- [ ] **SMART-13** [P1] End-to-end pass across the four buckets — a fully-native query (bucket A, must work day 1 with no new infra), an amenity query (B), a proximity query (C, assert POIs actually constrain results), and a rental/subjective query (D, assert honest degrade with **no misleading empty set**). Assert **no filter reaches SQL that isn't in the validated `ParsedQuery`** — the check that the LLM can't inject. QA `M` · deps: SMART-09, SMART-07 · _manual + preview_

## ✨ Vicinus IQ Brief — personalized dashboard brief (2026-07-21)
_A "Here's what moved while you were away" card at the top of the dashboard: 2–3 sentences of AI-written prose over **real** alert + preference data, plus deterministic CTA chips. Replaces the fabricated `WelcomeBanner` copy (closes **JUL21FIX-10/11**)._

_**Architecture — the one rule that governs every task below: the LLM writes prose, it never computes facts.** Every figure that reaches the page (price, delta, counts, addresses) is produced by `buildBriefFacts()` in plain TS/SQL and passed *into* the prompt. Gemini only phrases them. A model-authored price is both a UX bug and a **DDF compliance** problem, so the facts object is the single source of truth and the template fallback renders from that same object._

_**Scope decision (2026-07-21):** builds on a structured preference profile rather than the raw JSON blob, and ships with Gemini copy from the start (template as fallback, not as phase 1). This **pulls DATA-15/16/17 forward out of Phase 2** — they were dep'd on `Phase 1` (the event pipeline), but nothing in the profile actually needs events, only `User.onboardingData`. DATA-15/16/17 are superseded by BRIEF-01/02/03 below; the feed ranker (DATA-18) then inherits the profile for free._

_**Not in scope:** the `AnalyticsEvent` pipeline, impression logging, and the learned ranker. The brief reads existing state (`Alert`, `SavedProperty`, `SavedSearch`); it needs no new event capture._

### Preference profile (supersedes DATA-15/16/17)
- [ ] **BRIEF-01** [P1] `UserPreferenceProfile` + `UserPreferredNeighbourhood` models + migration — structured, queryable projection of the onboarding blob: parsed budget band (→ `budgetMin`/`budgetMax` ints, not the `"$600K–$1M"` display string), `goal`, `timeline`, `homeType`, `bedroomsMin`, `mortgage`, `workingWithRealtor`, `openToNearby`, weighted lifestyle priorities, and preferred neighbourhoods as rows (not a JSON array) so the ranker can join on them later. Keys must match the wizard's literals exactly — `goal ∈ buy|sell|rent|exploring`, `timeline ∈ 3mo|3-6mo|6-12mo|researching`, `homeType ∈ condo|townhouse|detached|presale|any`, `lifestylePriorities ⊂ schools|commute|transit|parks|dining|walkability|quiet` (note `more` is a dead tile — never persist it). `M` · _api/prisma/schema.prisma_ · **supersedes DATA-15**
- [ ] **BRIEF-02** [P1] Dual-write the profile in `updateOnboarding` — parse + upsert the profile on every step save, **keeping `User.onboardingData` as the source of truth** and treating the profile as a derived projection, so parsing changes are re-derivable instead of lossy. Must tolerate partial data: the wizard PATCHes per-step and "Save & Exit" can land any subset, so every field is optional and a re-parse must never null out previously-parsed fields. Keep it non-throwing — onboarding saves are already deliberately non-blocking (`OnboardingWizard.saveStep` swallows errors), so a parse failure must not break the wizard. `M` · deps: BRIEF-01 · _api/src/modules/users/users.service.ts:342-353_ · **supersedes DATA-16**
- [ ] **BRIEF-03** [P2] Re-runnable backfill from existing `onboardingData` blobs — every current user has a blob and no profile. Because BRIEF-02 keeps the blob authoritative this is a **re-derive job, not a one-shot migration**: safe to run again after any parser change. `S` · deps: BRIEF-02 · _api/src/scripts/_ · **supersedes DATA-17**

### Brief API
- [ ] **BRIEF-04** [P1] `User.lastBriefAt` (nullable DateTime) + migration — the "since your last visit" boundary. Deliberately **not** `Alert.readAt`: that field belongs to `NotificationsPanel`, and `markAllRead` hard-**deletes** alerts (`alerts.service.ts:128-134`), so reading the brief must not touch it or the notification list silently empties. Null ⇒ fall back to a 7-day window. `S` · _api/prisma/schema.prisma_
- [ ] **BRIEF-05** [P1] `buildBriefFacts()` — the deterministic core, **no AI**. Pull unread `Alert` rows since `lastBriefAt` (indexed `[userId, readAt]`), join `SavedProperty`/`SavedSearch`/profile, and emit a typed facts object. **Salience ranking is where the quality lives:** a price drop on a *saved* property > a new listing matching a *saved search* > a new listing matching *profile neighbourhoods* > a status change. Cap at **3 highlights** — more and the paragraph turns to mush. Every number the page will render is computed here. `M` · deps: BRIEF-01, BRIEF-04 · _api/src/modules/brief/ (new)_
- [ ] **BRIEF-06** [P1] `BriefModule` + `GET /users/me/brief` — `ClerkAuthGuard`, returns `{ headline, body, highlights[], generatedAt, isFallback }`. Register in `app.module.ts`. `S` · deps: BRIEF-05 · _api/src/modules/brief/, api/src/app.module.ts_
- [ ] **BRIEF-07** [P1] Gemini copy generation — follow the established `AiService` pattern (`gemini-2.5-flash`, `responseMimeType: 'application/json'` + `responseSchema`, Redis-cached). Cache key must include the **latest alert id** so the copy regenerates only when something actually moved, not on a timer; TTL ~6h. Prompt rule, stated explicitly: *use only the figures provided; never invent or recompute a number.* ⚠️ Unlike `getPropertySummary`, a Gemini failure here must **not** 500 — fall back to the templated sentence from the same facts object so the dashboard always renders. `M` · deps: BRIEF-05 · _api/src/modules/brief/, api/src/modules/ai/ai.service.ts_
- [ ] **BRIEF-08** [P1] Deterministic CTA chips — built from the facts object with real hrefs, **never parsed out of the model's text**. ⚠️ **Route property links by DDF `ListingKey`, not the local cuid** — this is exactly the JUL21FIX-01/02 bug class and the brief is a new surface fed by the local `Property` table, so it will reproduce the 404 unless it uses the ListingKey from the start. `S` · deps: BRIEF-05 · related: JUL21FIX-02 · _api/src/modules/brief/_

### Frontend
- [ ] **BRIEF-09** [P1] `<VicinusBrief />` — **client** component with its own fetch + skeleton, following the `OpenHouseSchedule`/`useOpenHouseVisits` pattern rather than server-rendering it. Deliberate: a Gemini call on the server path would block the whole dashboard render, and personalization-in-the-server-render already bit us once (`5530d4d`). `M` · deps: BRIEF-06 · _src/components/dashboard/VicinusBrief.tsx (new), src/hooks/useBrief.ts (new)_
- [ ] **BRIEF-10** [P1] Rewire `WelcomeBanner` onto real data — delete `newUpdates = Math.min(savedCount, 3)` and the hardcoded "Chelsea and Aspen", mount `<VicinusBrief />`. **Closes JUL21FIX-10 and JUL21FIX-11** — do not fix those separately, this supersedes both. `S` · deps: BRIEF-09 · closes: JUL21FIX-10, JUL21FIX-11 · _src/components/dashboard/WelcomeBanner.tsx:9-33_
- [ ] **BRIEF-11** [P1] Honest empty state — zero unread alerts is the **common** case pre-launch, not the edge case. Decide before building, not after: either hide the card entirely or render a forward-looking variant (upcoming open houses / new-in-your-areas). Must never render an empty green box, and must never re-fabricate a count. `S` · deps: BRIEF-09 · _src/components/dashboard/VicinusBrief.tsx_
- [ ] **BRIEF-12** [P2] Advance `lastBriefAt` on **CTA click**, not on view — advancing on render makes the card empty on refresh, which reads as broken. Leave `Alert.readAt` untouched (see BRIEF-04). `S` · deps: BRIEF-09 · _api + FE_

### Verification
- [ ] **BRIEF-13** [P1] End-to-end pass — new user (no profile, no alerts), returning user with alerts, and Gemini-unavailable (fallback path). Assert **no figure on the page is absent from the facts payload** — the check that the LLM isn't inventing prices. QA `S` · deps: BRIEF-10, BRIEF-11 · _manual + preview_

## ☁️ VERTEX — move Gemini from the Developer API to Vertex AI on GCP (2026-07-21)
_Today all three Gemini call sites hit the **Gemini Developer API** with a raw `GEMINI_API_KEY`. Move them to **Vertex AI**, authenticated by a GCP service account, with an explicitly pinned region._

_**Why this is worth doing — data residency, not features.** The models and prompts are identical either way. What changes is where the request lands and under what terms: the Developer API gives no region control, while Vertex lets us pin an endpoint (`northamerica-northeast1`, Montreal) and sits under GCP's enterprise data terms. That is the same PIPEDA-defensibility argument that decided **DATA-01** (PostHog EU Cloud) — except here the payload is richer, since prompts carry property and (with BRIEF-07) user-preference data. Secondary wins: one billing/quota surface, IAM instead of a long-lived key, and Cloud Logging on the AI path._

_**Current state, verified this session:** `@google/generative-ai@0.24.1` — Google's **legacy** SDK, superseded by the unified `@google/genai`. Three `getGenerativeModel` sites across two services (`ai.service.ts:91,125`, `sell.service.ts:119`), each constructing **its own client** from `getOrThrow('GEMINI_API_KEY')`. All three pin `gemini-2.5-flash` and all three use `responseMimeType: 'application/json'` + `responseSchema`._

_**Do this before BRIEF-07 if both are in flight.** BRIEF-07 adds a *fourth* call site; migrating first means writing it once against the new client instead of porting it a week later. If BRIEF ships first, VERTEX-03's shared provider absorbs it — but the ordering is cheaper the other way._

- [ ] **VERTEX-01** [P1] **Decision gate — GCP project, region, billing.** Pick the project (new vs existing), confirm **`gemini-2.5-flash` is actually served in the chosen region** (model availability is regional and Montreal does not carry every model — verify before committing, this is the item most likely to invalidate the plan), and set a billing budget + alert. ⚠️ Vertex has **no free tier**, unlike the Developer API's — establish the expected monthly spend here rather than discovering it. Confirm the region choice against the same counsel reviewing DATA-02's PIPEDA posture. `S` · _decision/infra_
- [ ] **VERTEX-02** [P1] **Swap `@google/generative-ai` → `@google/genai`** — the legacy SDK is deprecated and is *not* the migration target. The unified SDK talks to **both** backends, so switching to Vertex afterwards is a constructor flag (`vertexai: true`, `project`, `location`) rather than a second rewrite. Do this as a **pure like-for-like port on the existing API key**, no behaviour change, so the SDK swap and the backend swap stay independently revertible. Note the call shape differs (`ai.models.generateContent({model, contents, config})` vs `getGenerativeModel().generateContent()`) — confirm the current signature against the installed package's docs rather than from memory. `M` · deps: VERTEX-01 · _api/package.json, api/src/modules/ai/ai.service.ts, api/src/modules/sell/sell.service.ts_
- [ ] **VERTEX-03** [P1] **Extract one shared Gemini provider** — three services currently construct three clients from the same env var, so every auth change is a three-place edit and the fourth (BRIEF-07) would make four. Introduce a `@Global` provider (mirroring the existing `RedisService` pattern) owning client construction, model id, region and credentials; services inject it and pass only prompt + schema. This is the task that makes VERTEX-05 a one-line flip. `M` · deps: VERTEX-02 · _api/src/common/ or api/src/modules/ai/_
- [ ] **VERTEX-04** [P1] **GCP service account + credentials on Railway** — create the SA, grant **`roles/aiplatform.user`** (not Editor), and wire Application Default Credentials. ⚠️ Railway has no workload-identity federation to GCP, so this means a **JSON key in an env var** — a long-lived credential that must be set directly in Railway (never committed, never in `.env.example` beyond a placeholder) and given a documented rotation owner. Local dev should use `gcloud auth application-default login` rather than sharing the key. `M` · deps: VERTEX-01 · _Railway env, GCP IAM_
- [ ] **VERTEX-05** [P1] **Flip to the Vertex backend behind config** — `GEMINI_BACKEND=developer|vertex` (default `developer`) reading `GOOGLE_CLOUD_PROJECT` / `GOOGLE_CLOUD_LOCATION`, so production can be reverted by an env change rather than a redeploy. ⚠️ **`SellService.fallbackValuation` keys off "`GEMINI_API_KEY` is a placeholder"** (`sell.service.ts:161`) — under Vertex that variable no longer exists, so the placeholder check must be re-expressed against the new provider or the fallback silently stops arming. `S` · deps: VERTEX-03, VERTEX-04 · _api/src/modules/ai/, api/src/modules/sell/sell.service.ts:161_
- [ ] **VERTEX-06** [P1] **Parity + latency verification on all three call sites** — property summary, neighbourhood summary, valuation. Assert **structured output still conforms**: `responseSchema` enforcement has historically differed subtly between the two backends, and all three services `JSON.parse()` the response with no schema validation after the fact, so a drift shows up as a runtime crash rather than a warning. Compare p95 latency (a pinned Montreal region may be slower than the Developer API's anycast) and confirm the Redis cache still absorbs it. QA `S` · deps: VERTEX-05 · _manual + preview_
- [ ] **VERTEX-07** [P2] **Docs + `.env.example` + rotation runbook** — replace the lone `GEMINI_API_KEY=` line with the Vertex variables, and write down which SA, which region, and who rotates the key. `S` · deps: VERTEX-05 · _api/.env.example:19, docs/_

## 🩹 July 21FIX — Neighbourhood detail page + homepage hero copy (2026-07-21)
_Raised from a live-site report: on a Neighbourhood detail page, clicking any card in the **"Live listings"** carousel lands on Next's **404 — This page could not be found**. Root cause verified in code this session._
_**Diagnosis:** the property detail page resolves a listing **DDF-live by ListingKey** — `getPropertyDetail()` calls `GET /search/listing/{id}` (`src/lib/api/properties.ts:158-159`) and returns `null` on 404, which the page turns into `notFound()`. But the neighbourhood cards navigate with `router.push('/properties/' + listing.id)`, and `listing.id` is the **local Prisma `Property.id` (cuid)**, not the ListingKey — `getDetailListings()` maps `id: p.id` while putting the real ListingKey in the sibling fields `slug` and `mlsNumber` (`api/src/modules/neighbourhoods/neighbourhoods.service.ts:398-411`). Every card therefore requests a key DDF has never heard of ⇒ guaranteed 404, for all cards, always. The comment on that mapping ("The FE routes property links by DDF ListingKey") is exactly right — the FE just doesn't use the field it was given._
_**Not the cause** (ruled out): the `/properties/[id]` route exists; `PropertiesService` (`properties.service.ts:202`) already accepts either id form — but the detail page doesn't go through that service._

- [x] **JUL21FIX-01** [P0] _(Bug)_ **Route neighbourhood "Live listings" cards by DDF ListingKey, not the local cuid** — the fix is one field in each card component: navigate to `/properties/${listing.slug}` (the ListingKey the API already returns) instead of `listing.id`. Both carousel implementations have the same bug and both are in use — fix them together. While there, skip/guard cards whose `slug` is null so a missing key renders a non-clickable card rather than another 404. `S` · _src/components/neighbourhood/LiveListings.tsx:25-26, src/components/neighbourhood/LiveListingsCarousel.tsx:25_ — **Done 2026-07-22.**
- [x] **JUL21FIX-02** [P1] _(Bug)_ **Audit every other `/properties/{…}` link for the same cuid-vs-ListingKey mismatch** — the two neighbourhood carousels are unlikely to be the only offenders; any surface fed by the local Postgres `Property` table (dashboard saved/visited cards, alerts, similar-listings, nearby open houses, `getFeatured`) can pass a cuid into a route that resolves DDF-live. Grep every `/properties/` href + `router.push` and confirm each one passes a ListingKey. Note `PropertiesService.getFeatured` already does this correctly (`ddfListingKey ?? id`, `properties.service.ts:177`) — use it as the reference. `M` · _src/components, src/app_ — **Done 2026-07-22.**
- [x] **JUL21FIX-03** [P2] _(Improvement)_ **Stop a bad listing id from rendering a bare Next 404** — even once the ids are right, a delisted/sold listing will 404 between the 15-min sync and the DDF fetch. Replace the raw `notFound()` on the property page with a branded "This listing is no longer available" state offering a link back to search / the neighbourhood, and log the unresolved key so silent 404s are visible rather than only reported by users. Depends on JUL21FIX-01 landing first (otherwise it masks the real bug). `S` · _src/app/(main)/properties/[id]/page.tsx_ — **Done 2026-07-22.**

### Also on the same screen (from the screenshots)
_Two more defects visible on the Neighbourhood detail page in the same session. Both traced to source; both are data-layer gaps, not rendering bugs._

- [x] **JUL21FIX-04** [P1] _(Bug)_ **Rental cards read "Price on request" — the lease price is stored but never selected** — the DDF sync correctly maps **both** `price: ListPrice` **and** `leaseAmount: LeaseAmount` / `leaseFrequency` (`ddf-property.sync.ts:115-117`), and `DdfQueryService` already knows a rental's asking price lives in `LeaseAmount` (`ddf-query.service.ts:130-131, 306-309`). But `getDetailListings()` selects only `price` and maps `price: p.price ?? 0`, so every lease listing collapses to `0` and the card falls back to the "Price on request" string — exactly what the **FOR RENT** card in the screenshot shows. Select `leaseAmount` + `leaseFrequency`, fall back `price ?? leaseAmount`, and render the frequency suffix (e.g. "$2,400/mo") rather than a bare figure. ⚠️ Confirm against DDF: per prior finding, DDF carries **commercial leases only** — check whether these cards should appear on a consumer neighbourhood page at all before polishing their price display. Note some non-rental cards in the screenshot are also priceless, which may be a genuine null `ListPrice` — verify against the raw feed for those two ListingKeys before assuming one cause. `M` · _api/src/modules/neighbourhoods/neighbourhoods.service.ts:378-411, src/components/neighbourhood/LiveListings.tsx_ — **Done 2026-07-22.**
- [ ] **JUL21FIX-05** [P1] _(Missing)_ **Local information tiles read "0 nearby" / "0 places" — POI ingestion never runs on a schedule** — the counts are real: `getLocalEssentials()` reads `NeighbourhoodPoi` and returns empty buckets when the table has no snapshot for that neighbourhood. `PoiIngestionService.ingestAllNeighbourhoods()` exists and works, but its **only caller is the manual script** `api/src/scripts/compute-neighbourhood-scores.ts:65` — there is no `@Cron`, so any neighbourhood added since the last manual run has zero POIs forever. Backfill by running the script, then schedule the ingest (POIs change slowly — weekly is ample) so new neighbourhoods self-populate. Note livability/walkability scoring reads the same table, so empty POIs likely degrade those scores too — check what they render today for an unpopulated neighbourhood. `M` · _api/src/modules/neighbourhoods/poi-ingestion.service.ts, api/src/scripts/compute-neighbourhood-scores.ts_
- [x] **JUL21FIX-06** [P2] _(Improvement)_ **Give the Local information tiles an honest empty state** — independent of JUL21FIX-05: a tile that says "0 nearby" reads as *"this neighbourhood has no schools"* when it actually means *"we haven't ingested this data"*. Suppress the count (or show "Data coming soon") when the bucket is empty, and don't present the tile as an active affordance if tapping it opens an empty list. `S` · _src/components/neighbourhood/LocalInfoTiles.tsx:44-51_ — **Done 2026-07-22.**

### Homepage hero copy change
_Requested change: hero headline **"The Art of Intelligence in Living."** → **"Bringing you closer to Home."**, and the search label **"Find your home"** → **"Swipe. Connect. Own"**._
_**Gotcha found while locating the copy:** `src/lib/strings.ts` defines `HOMEPAGE_HERO_TITLE_LEAD/ACCENT/TRAIL` and `HOMEPAGE_HEROSEARCH_LABEL` — but **nothing imports them**; both components hardcode the literals. Change one place and the copy looks updated while the constants rot (or vice-versa). Either update both, or delete the dead constants as part of the change._

- [ ] ~~**JUL21FIX-07**~~ **superseded by JUL21FIX-12** (copy sheet supplies the exact fragments + accent word) — original: **Hero headline → "Bringing you closer to Home."** — the `<h1>` is built as three hardcoded fragments with `Intelligence` wrapped in a lime accent span. The new line has no natural mid-sentence accent word, so **decide the treatment before editing**: accent **"Home."** (keeps the lime and echoes the wordmark's lime full-stop), or drop the accent span and set the line plain white. Update the matching `HOMEPAGE_HERO_TITLE_*` constants in `strings.ts` (or remove them — see note above). Shorter line at `text-7xl` — sanity-check it doesn't leave an awkward single-word second row on desktop or overflow on mobile. `S` · _src/app/page.tsx:161-166, src/lib/strings.ts:49-51_
- [ ] ⚠️ **JUL21FIX-08** [P1] _(Improvement)_ **BLOCKED — needs a decision.** The copy sheet (JUL21FIX-12…14) assigns **"Swipe. Connect. Own"** to `HOMEPAGE_HIGHLIGHTS_TITLE` (the Curated Highlights section), **not** to the hero search label, and leaves `HOMEPAGE_HEROSEARCH_LABEL: 'Find your home'` unchanged. So either the tagline moved to Highlights and the search label stays as-is (⇒ close this task, keep only the a11y fix below), or it appears in **both** places (⇒ confirm the repetition is intentional). Confirm before editing. Original ask: **Search label → "Swipe. Connect. Own"** — replace the hardcoded `Find your home` and update `HOMEPAGE_HEROSEARCH_LABEL`. ⚠️ **Accessibility catch:** this element is `id="hero-search-label"` and the form is `aria-labelledby="hero-search-label"` (added by DSGN-01), so it is the *only* description a screen reader gets for the search. "Swipe. Connect. Own" is a brand tagline, not a form label — keep the visible tagline but give the form its own `aria-label` (e.g. "Search properties by neighbourhood, city, or postal code") instead of pointing at the tagline. ⚠️ **Copy check:** "Swipe" promises a swipe interaction the homepage doesn't have — confirm that's intentional brand voice (or forward-referencing the feed) and not a promise the hero can't keep. `S` · _src/components/landing/HeroSearchBar.tsx:81-92, src/lib/strings.ts:54_

- [x] **JUL21FIX-09** [P1] _(Bug)_ **"Find your home" label is white-on-cream and effectively invisible on the dashboard** — `HeroSearchBar` hardcodes `text-white` on the label (`HeroSearchBar.tsx:84`), which is correct over the homepage's darkened hero photo but **the same component is reused on the dashboard** (`WelcomeBanner.tsx:27`) against a cream `#FAF9F6` page — white on near-white, far under the WCAG AA 4.5:1 minimum. Root cause is that the component assumes a dark backdrop rather than the placement being wrong. Add a `tone` prop (`'on-dark'` | `'on-light'`, default `'on-light'`): homepage passes `on-dark` and keeps white; dashboard renders the label in the page's dark green `#1C3829` (matches the `Welcome back` heading and the Search button, so it reads as part of the theme rather than a new colour). Verify the fixed contrast ratio, and while there confirm nothing else in the bar relies on the dark backdrop. **Coordinate with JUL21FIX-08** — same element, so land them together to avoid one reverting the other. `S` · _src/components/landing/HeroSearchBar.tsx:81-92, src/components/dashboard/WelcomeBanner.tsx:25-28_ — **Done 2026-07-22.**

- [~] **JUL21FIX-10** [P1] _(Bug)_ **Dashboard welcome line advertises "Chelsea and Aspen" — cities we have no listings for** — hardcoded placeholder copy shipped to signed-in users (`WelcomeBanner.tsx:20-23`). Chelsea (QC/UK) and Aspen (Colorado) are outside the BC/DDF inventory entirely, so the line invites a click into guaranteed zero results and reads as a US template. **Prefer deriving over re-hardcoding:** `data.saved[].property.city` is already on the payload (`types/dashboard.ts:22`), so name the user's own two most-frequent saved cities, falling back to real BC markets (e.g. Vancouver · Kelowna) only when they have no saves yet. A fresh hardcode of BC city names is acceptable as a same-day stopgap, but it re-breaks the moment inventory shifts. `S` · _src/components/dashboard/WelcomeBanner.tsx:19-23_ — **Interim fix landed 2026-07-22** (derived cities + truthful saved-count line). Still superseded by BRIEF-10, which replaces the whole sentence with the brief card.
- [~] **JUL21FIX-11** [P2] _(Bug)_ **The "N new updates" count in the same sentence is fabricated** — `newUpdates` is `savedCount > 0 ? Math.min(savedCount, 3) : 2` (`WelcomeBanner.tsx:12`): it counts nothing, and a user with **zero** saved properties is still told they have **2 new updates** "since your last visit". Same sentence as JUL21FIX-10, so fix together. Either compute a real delta (price drops / status changes on saved listings since `lastVisit` — the alerts pipeline already detects these, see `ddf-property.sync.ts:93-94`) or drop the claim and write a line that doesn't assert a number. `S` · _src/components/dashboard/WelcomeBanner.tsx:11-23_ — **Interim fix landed 2026-07-22** (derived cities + truthful saved-count line). Still superseded by BRIEF-10, which replaces the whole sentence with the brief card.

## 🚨 DDFREV — post-deploy verification + open risks (2026-07-22)
_Raised while auditing the **live** vicinus.ca ahead of showing the site to CREA/DDF. Three live attribution bugs were found and fixed (search deep-links, homepage featured cards, `$0` price) and pushed to `main` at `296c30c`. **Everything below is what that push does NOT cover.** DDFREV-01 is the one that matters most: it is the difference between reporting analytics to CREA and reporting nothing at all._

- [ ] **DDFREV-01** [P0] _(Risk)_ **Confirm `DDF_DESTINATION_ID` is actually set in Railway** — `CreaAnalyticsService.logEvent()` returns early with only a `debug` log when the var is missing (`crea-analytics.service.ts`). If it is unset in production we are reporting **zero** view/click/lead events to CREA while believing we are compliant — the central REAW obligation, and silent by design. Cannot be checked from the repo. Verify in the Railway dashboard, then confirm events actually arrive by watching the API logs while loading a listing page. `S` · _Railway env, api/src/modules/analytics/crea-analytics.service.ts_
- [ ] **DDFREV-02** [P0] _(Verification)_ **Re-audit the live site after the deploy lands** — the fixes in `296c30c` are pushed but were verified only by `tsc` + `next build`, never in a browser against prod. Re-check on live: (a) every "Powered by REALTOR.ca" badge on `/search` resolves to `realtor.ca/real-estate/…` and not `realtor.ca/`; (b) homepage "Curated Highlights" cards show agent/brokerage/MLS®/REALTOR.ca; (c) no `$0` renders anywhere in search or feed; (d) the homepage card link still works and the attribution anchor does not break hydration (the card was restructured to avoid nested `<a>`). Fast check in devtools console: `[...document.querySelectorAll('a[href*="realtor.ca"]')].filter(a=>/realtor\.ca\/?$/.test(a.href)).length` should be `0`. `S` · _live site_
- [ ] **DDFREV-03** [P1] _(Verification)_ **Smoke-test the CREA-01…04 hardening against a running API** — the analytics rate limit, the signed-cookie visitor id, the listing-key validation and the drain queue all shipped **compile-verified only**; none has run against a live DB/Redis or a real CREA endpoint. Highest-risk piece is CREA-02: the cookie is `SameSite=None; Secure` in production, so if `COOKIE_SECRET` is unset or the cookie is blocked, `resolveVisitorId` mints a **new id on every request** — which silently defeats CREA's dedup, the exact failure the fix was meant to prevent. Verify a `vic_vid` cookie is set once and reused. `M` · _api/src/modules/analytics/*_
- [ ] **DDFREV-04** [P1] _(Risk)_ **Set `COOKIE_SECRET` in Railway** — `main.ts` falls back to a hardcoded dev secret when it is missing, so signed cookies would be forgeable in production. Ship alongside DDFREV-03. `S` · _Railway env, api/src/main.ts_
- [ ] **DDFREV-05** [P1] _(Missing)_ **Audit the remaining listing surfaces for attribution** — only `/search`, `/` and the property detail page were checked against live. Still unverified: `/feed`, neighbourhood detail live listings, dashboard saved/visited cards, map popups, nearby open houses. Each renders DDF data and each therefore needs agent/brokerage/MLS® + a deep-linked REALTOR.ca badge. The homepage bug (attribution absent entirely) proves this class of gap exists; assume others until checked. `M` · _src/components/{feed,neighbourhood,dashboard,search,property}_
- [ ] **DDFREV-06** [P1] _(Risk)_ **The reconciliation cron is still inert** — `DDF_RECONCILE_DRY_RUN` defaults to dry-run (CREA-07b), so sold/cancelled listings are **not** being deleted and stale inventory can still be advertised, which is the DDF breach the cron exists to prevent. Watch a few nightly dry-run logs, confirm the delete counts look sane, then set `DDF_RECONCILE_DRY_RUN=false` to arm it. Do not arm it unwatched. `S` · _Railway env, api/src/modules/ddf-sync/ddf-reconciliation.sync.ts_
- [ ] **DDFREV-07** [P2] _(Risk)_ **Set the Airtable env vars or accept the waitlist mirror is off** — `AIRTABLE_API_KEY` / `AIRTABLE_BASE_ID` unset means `pushRealtorSignup` silently no-ops. Postgres is still the system of record, so no data is lost, but ops will not see signups. `S` · _Railway env_
- [ ] **DDFREV-08** [P2] _(Improvement)_ **Missing brokerage on some live listings** — at least one live search card ("305 4759 VALLEY DRIVE") rendered with no brokerage line while its neighbours had one, i.e. `office` came back null from DDF for that listing. Decide the intended behaviour when DDF omits the brokerage: fall back to the agent, or suppress the MLS® line too. Currently the card is partially attributed. `S` · _api/src/modules/ddf-sync/ddf-query.service.ts (resolveOfficeName)_

### Homepage copy sheet (supplied 2026-07-21)
_A copy sheet was supplied listing exact new values for three homepage string groups. **Same structural gotcha throughout:** every one of these constants already exists in `src/lib/strings.ts` but **nothing imports them** — `src/app/page.tsx` hardcodes the visible literals. Updating `strings.ts` alone changes nothing on screen. Each task below therefore covers **both** the constant and the JSX, and the cleanest close-out is to have `page.tsx` import from `strings.ts` so the next copy change is a one-file edit. Watch the quoting: the sheet mixes straight and curly quotes — use straight `'` in TS and `&apos;` in JSX (as the existing "Luxury isn't" line does)._
_⚠️ **Only the first page of the supplied PDF was readable** (the file sits in WhatsApp's sandboxed container, which this session can't open — the values below were transcribed from a screenshot of page 1). If the sheet runs longer, send the rest and I'll extend this list._

- [x] **JUL21FIX-12** [P1] _(Improvement)_ **Hero headline → "Bringing You Closer to Home."** — supersedes JUL21FIX-07 and settles its open question: the accent word is **"You"** (mid-sentence, so the existing three-fragment lime-span structure is preserved exactly — only the words change). Set `HOMEPAGE_HERO_TITLE_LEAD: 'Bringing'`, `_ACCENT: 'You'`, `_TRAIL: 'Closer to Home.'` and update the matching JSX. Note the sheet capitalises **"You"** and **"Closer"** — differs from the earlier verbal ask ("Bringing you closer to Home."); the sheet wins unless told otherwise. `S` · _src/lib/strings.ts:48-51, src/app/page.tsx:161-166_ — **Done 2026-07-22.**
- [x] **JUL21FIX-13** [P1] _(Improvement)_ **Curated Highlights → "Swipe. Connect. Own" / "Real estate, centred around you."** — set `HOMEPAGE_HIGHLIGHTS_TITLE: 'Swipe. Connect. Own'` (was `'Curated Highlights'`) and `HOMEPAGE_HIGHLIGHTS_SUBTITLE: 'Real estate, centred around you.'` (was `'Active, intelligently-vetted opportunities.'`); `_VIEWALL: 'View all'` is unchanged. The sheet is silent on `HOMEPAGE_HIGHLIGHTS_EYEBROW: 'Hand-picked'` — confirm it stays, since "Hand-picked" above "Swipe. Connect. Own" is a slight tonal mismatch. ⚠️ Same "Swipe" caveat as JUL21FIX-08: the homepage has no swipe interaction, so the title promises a gesture this section doesn't offer — confirm it's forward-referencing the feed. `S` · _src/lib/strings.ts:71-74, src/app/page.tsx:198-203_ — **Done 2026-07-22.**
- [x] **JUL21FIX-14** [P1] _(Improvement)_ **Contextual Living → "Understand the vicinity"** — set `HOMEPAGE_CITIES_TITLE: 'Understand the vicinity'` (was `'Contextual Living'`) and `HOMEPAGE_CITIES_BODY: 'A new home means new streets, new neighbours, new cafes — a whole new way of living. Discover yours.'`; `_ALLLINK` and both badge strings are unchanged. Keep the em dash as a literal `—` (matches existing usage) and note the body is ~15 chars longer than the current line inside a `max-w-md` centred block — check it doesn't reflow to an awkward orphan. Title is sentence case where the current one is title case; that's what the sheet says, flagging in case it's a typo. `S` · _src/lib/strings.ts:76-82, src/app/page.tsx:227-234_ — **Done 2026-07-22.**

## 🔐 CREA FIX tasks — DDF compliance hardening (2026-07-21)
_Raised from a review of the uncommitted `feat/ddf-compliance` branch. **Context:** Vicinus holds DDF access at the **REAW** tier (Real Estate Advertising Website on the National Shared Pool), where accurate analytics reporting and correct attribution are terms of the data agreement — not internal metrics. Every item below is a gap between what the branch does and what the DDF contract requires. **Blast radius:** both the consumer site and the (planned) realtor portal are downstream of the single DDF feed; suspension ends the product, not just a feature. None of CREA-01…04 are user-visible, so they ship independently of any frontend release. Code locations verified this session._

### Analytics endpoint — reporting integrity (P0)
_**Behaviour today:** `POST /analytics/listing-event` is public, unauthenticated and unthrottled, and every field is caller-supplied. Anyone on the internet can assert that an event happened on vicinus.ca, and CREA receives it stamped with our `DestinationID`. The service docstring claims client throttling is unnecessary because "CREA dedups by UUID within 5-minute windows" — but the **caller chooses the UUID**, so rotating it defeats the dedup entirely. **Target behaviour:** Vicinus reports to CREA only what Vicinus actually observed — the endpoint should be an internal sensor, not a public megaphone. **Note:** CORS (`main.ts:17`) does not mitigate this; it is a browser policy and is ignored by `curl`/scripts._

- [x] **CREA-01** [P0] _(Security)_ **Rate-limit the analytics endpoint + trust the proxy** — `@nestjs/throttler` is **not installed** and the setup is a commented-out TODO. Install it, wire `ThrottlerModule.forRoot` in `AppModule` + `useGlobalGuards` in `main.ts`, and put a tight per-route `@Throttle` on `listing-event` (~30/min/IP is generous for a human). Also set `app.set('trust proxy', 1)` — without it `x-forwarded-for` is attacker-controlled (`analytics.controller.ts:24`) and per-IP limiting is trivially evaded. Highest value per minute spent; do this first. `S` · _api/src/main.ts:10-11,25, api/src/app.module.ts, api/src/modules/analytics/analytics.controller.ts_
- [x] **CREA-02** [P0] _(Security)_ **Issue the visitor UUID server-side instead of accepting it from the body** — root-cause fix. Today `getDeviceId()` mints a `crypto.randomUUID()` into `localStorage` and the browser sends it up, so the identity is fully caller-chosen and CREA's 5-minute dedup can never bind. Instead issue the identifier on first visit as an `HttpOnly` signed cookie, read it from the cookie in the controller, and **ignore `dto.uuid`** entirely (drop the field, or keep it and discard). Restores dedup and makes the visitor identity non-forgeable. `M` · _api/src/modules/analytics/analytics.controller.ts, dto/listing-event.dto.ts, src/lib/deviceId.ts_
- [x] **CREA-03** [P1] _(Security)_ **Validate `listingKey` before forwarding to CREA** — `listingKey` is accepted as any string ≤64 chars, so events can be reported for listings we don't serve or that don't exist. Check the key against the Redis `listing:<key>` cache / `Property` table and drop unknown keys before the outbound call. One cache lookup; kills fabricated-listing spam. `S` · _api/src/modules/analytics/crea-analytics.service.ts_
- [x] **CREA-04** [P2] _(Improvement)_ **Queue CREA LogEvents calls instead of 1:1 fan-out** — each inbound request currently triggers an immediate outbound GET to `analytics.crea.ca`, so a burst amplifies straight through to CREA under our DestinationId. Push events onto a queue and drain at a controlled rate. Not needed at current traffic — revisit at scale. `M` · _api/src/modules/analytics/crea-analytics.service.ts_

### DDF obligations — incomplete on the branch
- [x] **CREA-05** [P1] _(Missing)_ **`Click` and `email_realtor` events have no callers** — `CreaEventType` and the DTO support all three event types but only `view` is wired (via `ListingViewTracker` on the property detail page). The REAW tier requires click-through and lead reporting, so this is a live compliance gap, not a nice-to-have. Fire `Click` on listing card → detail navigation and `email_realtor` on the contact-agent action. `M` · _src/lib/api/analytics.ts, src/components/property/AgentCard.tsx, src/components/property/PropertyCell.tsx_
- [x] **CREA-06** [P1] _(Bug)_ **`realtorUrl` never mapped for dashboard cards — attribution deep-links fall back to the REALTOR.ca homepage** — `DashboardProperty` gained the optional field and `SavedProperties`/`VisitedProperties` pass `property.realtorUrl` into `PropertyCell`, but `users.service.ts` (`toDashboardProperty` / `ddfToDashboardProperty`) never selects or maps it. Optional type ⇒ no compile error, silent fallback at runtime. Likely the same for `OpenHouseProperty.realtorUrl` in `NearbyOpenHouses` — verify both. `S` · _api/src/modules/users/users.service.ts:~70-110_

### Reconciliation — destructive cron safety
_**Behaviour today:** the 15-min property sync is upsert-only and never removes, so sold/cancelled/expired listings accumulate forever — which DDF terms prohibit. The new `@Cron(EVERY_DAY_AT_4AM) reconcileListings()` fixes that by deleting any local `Property` absent from CREA's `PropertyReplication()` master list. It guards against an **empty** master list, but the list arrives **paginated** via `@odata.nextLink` — if the chain breaks partway and the response still parses, the job holds a short-but-nonzero set and deletes everything missing. The system currently cannot distinguish "most listings sold overnight" from "we only received part of the answer", and treats both as an instruction to delete. **It has never been run; its first execution is against prod, unattended, at 4AM.** **Target behaviour:** delete only when confident the master list is complete — a partial answer must delete nothing._
_**Risk framing:** *not* reconciling is the actual contract violation (advertising sold inventory); reconciling *wrongly* is an operational failure, not a compliance one. **Blast radius is contained** — `SavedProperty`/`VisitedProperty.propertyId` are plain DDF ListingKey strings (not FKs) so user saves survive, and `Alert.propertyId` is `onDelete: SetNull` so alerts are orphaned but not lost. Recovery is a re-sync, not a restore. What degrades meanwhile: neighbourhood inventory counts + listings, valuation comparables, dashboard cards, alert matching (verified consumers of the local `Property` table: `properties.service.ts`, `neighbourhoods.service.ts`, `users.service.ts`, `alerts.service.ts`)._

- [x] **CREA-07a** [P1] _(Risk)_ **Percentage delete ceiling** — generalize the existing zero-key guard from "total failure" to "implausible": abort the run when `stale.length / local.length > ~0.3`, logging the counts. Highest value per line in this section; tune the threshold once a few real runs show normal daily churn (expect low single-digit %). `S` · _api/src/modules/ddf-sync/ddf-reconciliation.sync.ts:44-52_
- [x] **CREA-07b** [P1] _(Risk)_ **`DDF_RECONCILE_DRY_RUN` env flag** — compute the diff, log what *would* be deleted, delete nothing. Run in prod for a few nights, read the logs, then flip off. This is what makes the never-yet-run first execution safe. `S` · _api/src/modules/ddf-sync/ddf-reconciliation.sync.ts_
- [x] **CREA-07c** [P1] _(Bug)_ **Detect truncated pagination** — the root cause, not the symptom. Compare collected key count against `@odata.count` when CREA returns it, or against the previous successful run's `masterCount` in `DdfSyncLog`; a sudden large drop means "incomplete answer", not "mass expiry". Add a retry around each page fetch so one transient failure can't silently shorten the master list. `M` · _api/src/modules/ddf-sync/ddf-reconciliation.sync.ts:88-116 (fetchMasterListingKeys)_
- [x] **CREA-07d** [P2] _(Improvement)_ **Alert on abort** — a skipped run currently writes only a log line nobody reads, so a silently-aborting reconciliation is indistinguishable from a healthy one (and we're back to advertising sold listings unaware). Write `status: 'error'` to `DdfSyncLog` on abort and notify. Do alongside CREA-07a. `S` · _api/src/modules/ddf-sync/ddf-sync.service.ts:94-120_
- [x] **CREA-08** [P2] _(Improvement)_ **Fix stale docstring on `DdfSyncService`** — the class comment still states `syncMembersAndOffices()` "remains on its own manually-triggered cadence", but it now carries `@Cron(EVERY_DAY_AT_3AM)`. One line. `S` · _api/src/modules/ddf-sync/ddf-sync.service.ts:13-25_

### Deploy ordering (not a task — a release constraint)
> ⚠️ **The API must ship before or with the frontend for this branch.** The Alerts tab sends `type=NEW_LISTING,PRICE_DROP,STATUS_CHANGE` (CSV); the DTO currently in prod rejects CSV with a **400**, so FE-before-API leaves the Alerts tab permanently empty. The analytics endpoint is the milder case — FE-without-API 404s per detail-page view, errors are swallowed, so it degrades to zero analytics rather than breaking.

## 🏠 Homepage Design Review — hero + footer + nav (2026-07-09)
_From a Product & Design review of the live vicinus.ca home page. Focus: make the hero search the unmistakable primary action, clarify the submit CTA, fill out the footer for trust/SEO, and de-emphasize the B2B nav item. Code locations verified this session. Sizes: S ≤0.5d · M 1–2d._

- [x] **DSGN-01** [P1] _(Improvement · H3)_ **Make the hero search the primary action** — **Done 2026-07-09.** Wrapped `HeroSearchBar` in a `max-w-3xl` container with an explicit **"Find your home"** label (`id=hero-search-label`, `aria-labelledby` on the form), enlarged the bar (input `px-5 py-4` + `text-base`, larger icons, `shadow-lg` on all three controls), added `aria-label`s to the input + price select, and updated the placeholder "…or ZIP" → "…or Postal Code" (Canadian) while here. FE type-check clean. `M` · _src/components/landing/HeroSearchBar.tsx_
- [x] **DSGN-02** [P2] _(Improvement · L4)_ **Clarify the search submit CTA** — **Done 2026-07-09.** Submit button relabelled **"Discover" → "Search"** (icon kept). `S` · _src/components/landing/HeroSearchBar.tsx_
- [~] **DSGN-03** [P2] _(Improvement · L2)_ **Flesh out the thin footer** — **Partial 2026-07-09.** Rebuilt the footer into a 3-col layout: **Brand** blurb, **Explore** (Buy · Sell · Neighbourhoods · Realtor Hub — all real routes, better SEO internal linking), **Legal** (Privacy Policy), plus a prominent **MLS®/CREA DDF® attribution** line and copyright. ⏳ **Terms / Contact / About intentionally omitted** — those pages don't exist yet (nor does `/privacy` — pre-existing 404), so linking them would ship 404s. **Follow-up:** create `/terms`, `/contact`, `/about`, `/privacy` static pages, then add them to `EXPLORE_LINKS`/Legal. FE type-check clean. `S` · _src/components/layout/Footer.tsx_
- [x] **DSGN-04** [P2] _(Improvement · L1)_ **De-emphasize "Realtor Hub" in consumer nav** — **Done 2026-07-09.** Added a `deEmphasize` flag to the nav-link model; Realtor Hub is now split out of the consumer links (Home · Buy · Sell · Neighbourhoods) behind a vertical divider and rendered muted + `text-xs` (`proLinkClass`) on desktop, and after the consumer links with a muted style on mobile. Still `hideWhenSignedIn`. FE type-check clean. `S` · _src/components/layout/Navbar.tsx_
- [x] **DSGN-05** [P1] _(Bug)_ **"Curated Highlights" live listings never render on the home page** — **Done 2026-07-09 (⚠️ needs API deploy).** Root cause was `GET /properties/featured` reading the empty/unsynced Postgres `Property` table while the rest of the app is DDF-OData-live. Rewrote `PropertiesService.getFeatured()` to query **DDF live** via the injected `DdfQueryService.searchProperties` (premium pass: BC · For Sale · Active · `minPrice 1M` · `minSqft 1000` · `beds 3`; relaxed BC/For-Sale/Active fallback when <3 photo-bearing results), filters to http(s)-image listings, takes 6, maps to the `FeaturedProperty` shape, **Redis-cached 10 min**. Wired `SearchModule` (which exports `DdfQueryService`) into `PropertiesModule`; removed the now-dead Prisma `getFeatured` + unused `firstImageUrl` helper. API type-check clean. `M` · _api/src/modules/properties/properties.service.ts, properties.module.ts_
- [x] **DSGN-06** [P2] _(Improvement)_ **Home "Contextual Living" — show cities (Vancouver · Kelowna · West Vancouver · Whistler), not neighbourhoods** — **Done 2026-07-09 (temporary hand-pick).** Section previously rendered `neighbourhoods.slice(0, 3)` as neighbourhood cards linking to `/neighbourhoods/[slug]`. Now renders **4 city cards** (`FEATURED_CITIES`), each linking to a city-scoped search `/search?q=<City>` (search reads `q` → `sp.q`). Each card's image/province is sourced from a representative neighbourhood in that city (case-insensitive exact `city` match, won't grab North/West Vancouver for "Vancouver") with a safe Unsplash fallback so cards render even when the neighbourhoods API returns `[]`. Removed the old `NeighbourhoodCard` (+ its random "Active Editorials" vanity badge) and unused `Neighbourhood` import; grid is 4-col; copy "preferred enclaves"→"preferred cities". TODO left for a proper curated selection. FE type-check clean; `GET /` SSR returns 200 (couldn't get a visual render — preview browser wouldn't load the local app in this env). `S` · _src/app/page.tsx (CityCard, FEATURED_CITIES, CITY_FALLBACK_IMAGE)_

## 🎨 Brand — Vicinus logo/wordmark rollout (2026-07-09)
_Brand guide (§02 Logo & Wordmark): the wordmark is the primary identity — **"Vicinus"** set in **Space Grotesk Bold** with a **single lime full-stop** (`.`), dark-green text (`#1C3829`) / lime dot. Today the mark is just literal text `Vicinus` in `font-heading` (Playfair serif — globals.css:146), no lime full-stop, and there is **no** favicon/app-icon or OG image. Code locations verified this session. Sizes: S ≤0.5d · M 1–2d._

- [x] **LOGO-01** [P1] **Add Space Grotesk + build a reusable `<Logo>` component** — load Space Grotesk (next/font) and wire a `--font-wordmark` var, then create `src/components/brand/Logo.tsx` that renders `Vicinus` + a lime full-stop (`<span className="text-lime-…">.</span>`), with `variant` (dark on light / white on dark) and size props. Single source of truth for every placement below. `M` · _src/app/layout.tsx, src/app/globals.css, new src/components/brand/Logo.tsx_
- [x] **LOGO-02** [P1] **Navbar** — replace the literal `Vicinus` text link with `<Logo>`; keep the solid/transparent colour switch (dark-green when `solid`, white otherwise). `S` · _src/components/layout/Navbar.tsx:80-88_
- [x] **LOGO-03** [P1] **Footer** — replace the `font-heading … Vicinus` span with `<Logo>` (white-on-dark variant). `S` · _src/components/layout/Footer.tsx:8_
- [x] **LOGO-04** [P1] **Onboarding wizard** — replace both wordmark spans (dark on step header, white on hero panel) with `<Logo>`. `S` · _src/components/onboarding/OnboardingWizard.tsx:179,215_
- [x] **LOGO-05** [P2] **Auth pages** — audit sign-in / sign-up for any header wordmark and swap to `<Logo>` for consistency. `S` · _src/app/(auth)/sign-in/[[...rest]]/page.tsx, src/app/(auth)/sign-up/[[...rest]]/page.tsx_
- [x] **LOGO-06** [P1] **Favicon / app icons + OG image** — there is no `icon`/`apple-icon`/`opengraph-image` in `src/app`. Add a lime-dot Vicinus favicon set (`icon.svg` + `apple-icon.png`) and a branded `opengraph-image` so the mark shows in browser tabs and social shares. `M` · _src/app/icon.svg, src/app/apple-icon.png, src/app/opengraph-image.*, src/app/layout.tsx (metadata)_

## 🐞 QA Bug Sweep #3 — Search + Property Detail (2026-07-08)
_Co-founder QA table: 3 Search items + 6 Property Detail items. Code locations verified this session. Sizes: S ≤0.5d · M 1–2d · L 3–5d._

### Search
- [ ] **SRCH-01** [P2] _(Improvement)_ **Map colour/texture** — the search map uses the muted `mapbox/light-v11` "soft sand" palette (`MAPBOX_STYLE`). Switch to a richer style with more colour/texture/variation (parks, water, roads, terrain). `S` · _src/components/search/MapView.tsx:30_
- [ ] **SRCH-02** [P2] _(Missing)_ **Neighbourhood showcase videos** — no area-specific video content in the search experience. Add short videos showcasing specific areas/neighbourhoods. Needs a content source + a `Neighbourhood.showcaseVideoUrl` field (or curated map) before FE work. Deferred — data/content dependency. `L` · _api/prisma/schema.prisma, src/components/search/*, neighbourhood detail_
- [ ] **SRCH-03** [P2] _(Missing)_ **Draw-your-own search area** — users can't draw a custom boundary on the map to constrain listings. Add a "draw a section" tool (`@mapbox/mapbox-gl-draw`) that emits a polygon, then filter listings/pins to the polygon. Requires a BE polygon/geo filter on the DDF-OData live path (or client-side point-in-polygon on returned pins as v1). Deferred — larger feature. `L` · _src/components/search/MapView.tsx, api/src/modules/ddf-sync/ddf-query.service.ts_

- [x] **SRCH-04** [P2] _(Improvement)_ **Beds/baths exact-match option** — the Beds & Baths filter only offered "N+" (minimum) with no way to match an exact count. Added a "Use exact match" checkbox in the popover that flips the button/chip labels `N+ → N` and threads an `exactBedsBaths` flag FE→param→DTO so the DDF OData filter emits `BedroomsTotal eq N` / `BathroomsTotalInteger eq N` instead of `ge`. `S` · _src/components/search/FilterPanel.tsx, src/store/searchStore.ts, src/types/search.ts, src/app/(main)/search/SearchPageClient.tsx, src/lib/api/search.ts, api/src/modules/search/dto/search-query.dto.ts, api/src/modules/ddf-sync/ddf-query.service.ts:127-128_

### Property Detail
- [ ] **PDP-01** [P1] _(Bug)_ **Virtual tour / video doesn't play** — the BE `mapProperty` already resolves `virtualTourUrl` (`VirtualTourURLBranded/Unbranded`) and `youtubeUrl` (Media "Video Tour Website"), but the FE `ApiListing`/`PropertyDetail` types drop both and no component renders them — so tours/videos never appear. Thread the fields FE→type→mapper and render a tour/video block (YouTube embed + branded-tour link). `M` · _src/lib/api/properties.ts:43,90-127, src/types/property.ts, src/app/(main)/properties/[id]/page.tsx, new src/components/property/VirtualTour.tsx_
- [ ] **PDP-02** [P1] _(Bug · Mobile)_ **Mobile layout doesn't stack cleanly** — `PropertyGallery` is a fixed `grid-cols-3 h-[440px]` regardless of viewport, so on phones the hero + 2 thumbs cram into 3 tiny columns. Make it stack: full-width hero + responsive height, thumbnails hidden/collapsed on mobile. `S` · _src/components/property/PropertyGallery.tsx:40-95_
- [ ] **PDP-03** [P2] _(Improvement)_ **Floor plan not prominently displayed** — floor plans (when present) aren't surfaced. Extract floor-plan media (DDF `MediaCategory` "Floor plan") in the BE mapper, thread a `floorPlans[]` field, and render a dedicated prominent block on the detail page. Deferred — needs BE media extraction + deploy. `M` · _api/src/modules/ddf-sync/ddf-query.service.ts, src/lib/api/properties.ts, src/components/property/*_
- [ ] **PDP-04** [P2] _(Improvement)_ **Add Street View + map buttons** — the detail page has no Street View or "view on map" affordance. Add a Google Street View button + a "view on map" button using the listing `latitude`/`longitude`. `S` · _src/app/(main)/properties/[id]/page.tsx, new src/components/property/PropertyLocationLinks.tsx_
- [ ] **PDP-05** [P2] _(Improvement)_ **Facts & Features spacing too loose** — `GroupBlock` rows use `justify-between` inside a 2-column layout, spreading label↔value to opposite edges (e.g. "Bathrooms" … "1"). Tighten to a compact left-aligned label/value pairing. `S` · _src/components/property/PropertyFacts.tsx:319-340_
- [ ] **PDP-06** [P2] _(Improvement)_ **Mortgage calculator missing asking price / PTT** — the calculator has no editable home-price input (can't see/change the asking price) and ignores BC Property Transfer Tax. Default an editable price input to the listing's asking price and add an optional PTT line (BC: 1% ≤$200k, 2% $200k–$2M, 3% >$2M). `M` · _src/components/property/MortgageAnalysis.tsx_

## ⚡ API cold-start — kill the first-load latency (2026-07-07)
_The single biggest source of "slow first load" (Feed, search, property detail — see QA-07) is the Railway API **cold-starting** after it sleeps: the first request after idle takes ~2s+ while the instance boots, vs ~0.4–0.9s warm. This is infra, not frontend — client-side parallelism (BUY-04) can't fix it. Config reviewed this session: `api/railway.toml` / `api/railway.json` set only build/start/restart; there is **no** min-instance or keep-warm field (App Sleeping is a per-service **dashboard** toggle), and there is **no health endpoint** to ping. Sizes: S ≤0.5d · M 1–2d._

- [ ] **OPS-01** [P1] Eliminate API cold-start — pick one: **(a)** disable **App Sleeping** on the Railway API service (dashboard → service → Settings; keeps it always-on — simplest, may cost more), or **(b)** keep sleeping but add an external **keep-warm cron** (e.g. Railway cron / UptimeRobot / GitHub Action) hitting a cheap endpoint every ~4 min so the instance never idles out. (b) depends on OPS-02. `S` · _Railway dashboard / infra_
- [x] **OPS-02** [P1] Add a lightweight `GET /health` endpoint — **Done 2026-07-09.** New `HealthModule`/`HealthController` returns `200 {status:'ok',uptime,timestamp}` with **no DB/DDF/Redis work** (verified: boots and responds 200 in ~5ms even with Redis unavailable). Set `healthcheckPath = "/health"` in both `api/railway.toml` + `api/railway.json` so deploys only cut over once the app is serving. Shallow subset of the planned [[project_data_pipeline]] **DATA-21**; `/health/deep` (Postgres/Redis checks) left to DATA-21. `S` · _api/src/modules/health/* (new), api/src/app.module.ts, api/railway.toml, api/railway.json_
- [x] **OPS-03** [P2] Fix stale start command in `api/nixpacks.toml` — **Done 2026-07-09.** `[start] cmd` corrected `node dist/main` → `node dist/src/main` (matches `railway.*` + `package.json start:prod`). `S` · _api/nixpacks.toml_

## 🗺️ Buy/Search — Map+Feed toggle + default city + map pins (2026-07-07)
_Founder request on the Buy (`/search`) screen: (1) collapse the view toggle to **just two options — Map and Feed — with Feed as the default**; (2) when no city/neighbourhood is searched and no device location is available, **default-load Vancouver listings**; (3) fix the bug where **no pins render on the map until a city is searched**. Code locations verified this session. Sizes: S ≤0.5d · M 1–2d._

- [ ] **BUY-01** [P1] Collapse the view toggle to **Feed + Map only, Feed default** — the toggle currently offers List · Both · Map. Reduce to two segments: **Feed** = the full-width results list (reuse the existing `'list'` view id, relabel "List"→"Feed"), and **Map** = the **existing split-pane list+map view** (reuse the current `'both'` view id, relabel "Both"→"Map"). The current **map-only** view (`'map'`) is dropped. Set the store default `viewMode` → `'list'` (Feed) so Buy opens on the Feed. Update the `ViewMode` type to the two surviving ids and delete the now-unused map-only branch; leave the split-pane rendering (Feed pane + map pane) as the "Map" view. `S` · _src/components/search/ViewToggle.tsx:6-10, src/store/searchStore.ts:84, src/types/search.ts:106, src/app/(main)/search/SearchPageClient.tsx:171-199_
- [ ] **BUY-02** [P0] **Fix: no map pins until a city is searched** — the map-pins query is gated `enabled: !!bbox` where `bbox` derives from `mapBounds`, but `mapBounds` is only set in `handleMoveEnd` (fires on user pan/zoom), so on first load of Buy the bounds stay `null` → pins query never runs → empty map. (The list pane works because its `bbox` is optional.) Fix: seed `mapBounds` as soon as the map is ready — add an `onLoad` handler on `<Map>` that reads `map.getBounds()` and calls `setMapBounds(...)` (same shape as `handleMoveEnd`), so pins load for the default viewport without requiring a pan. `S` · _src/components/search/MapView.tsx:106-128,164-179, src/app/(main)/search/SearchPageClient.tsx:145-155_
- [x] **BUY-05** [P1] **Unify the Feed into the Buy screen — Feed is a view, not a separate route.** `/search` and `/feed` were near-duplicate shells (same `Navbar`, overlapping filter bars, both hitting `searchProperties`). Consolidated: the Buy screen now owns one navbar + one `FilterPanel` + the Feed/Map toggle, and the toggle swaps the body between the vertical listing feed and the split-pane map — both driven by the same `searchStore` filters, so switching keeps the query. Extracted the feed scroll body into `FeedView` (reads shared params, `useInfiniteQuery`, no filter bar/navbar; portrait/expand toggle floats in the corner). Feed uses city-scoped params (device city / Vancouver default when browsing) since it has no map. `/feed` now redirects to `/search`; the redundant `FeedFilterBar` was deleted. The list query is gated to the Map view so the Feed doesn't double-fetch. `M` · deps: BUY-01 · _src/components/search/FeedView.tsx (new), src/app/(main)/search/SearchPageClient.tsx, src/app/(feed)/feed/page.tsx (→ redirect), src/components/feed/FeedFilterBar.tsx (deleted)_
- [ ] **BUY-04** [P2] **Prefetch pin data in the background so the map has data ready on switch** — the Feed→Map lag is the **API round-trip for pins**, not the Mapbox render (it's fine for the map canvas to draw when it appears). The pins query is gated `enabled: !!bbox`, and in Feed view there's no map → no `mapBounds` → the request only fires *after* the user switches, so they wait on the network. Fix: while still in Feed view, kick off the `['map-pins', …]` query in the background against the resolved default bbox (user location or Vancouver from BUY-03) — relax the `enabled: !!bbox` gate to fall back to that default bbox — so the pins are cached and render immediately when the map mounts. React Query dedupes, so this reuses the same cache the Map view reads (no double fetch). Map JS-chunk/Mapbox-init preloading is explicitly **out of scope** — UI draw latency is acceptable. `S` · deps: BUY-02, BUY-03 · _src/app/(main)/search/SearchPageClient.tsx:145-155_
- [ ] **BUY-03** [P1] **Default to Vancouver listings when no search + no device location** — priority: searched city > device geolocation (`LocationProvider` → `userCity`/`userCoords` + `mapCenter`) > **Vancouver fallback**. Today with no query and no `mapBounds`, the list falls back to a global "All Properties" set; the Vancouver coords at `searchStore.mapCenter` only recenter the *map*, they don't scope the *data*. Make Vancouver the data default: when `query` is empty AND no `userCoords`/`userCity` resolved, drive results from a Vancouver bbox/center so both the Feed and the map pins load Vancouver on first paint, and set the list `locationLabel` to "Vancouver" instead of "All Properties". Ensure this fallback is superseded the moment geolocation resolves or the user searches a city. `M` · deps: BUY-02 · _src/store/searchStore.ts:85-91, src/components/providers/LocationProvider.tsx, src/app/(main)/search/SearchPageClient.tsx:100-120,192_

## 🐞 QA Bug Sweep — Web (2026-07-06)
_Six user-reported web bugs (issue table 001–006). All `Web`. Code locations verified this session. Sizes: S ≤0.5d · M 1–2d._
_**Implemented 2026-07-06.** FE + API type-check clean. BUG-04/05 verified via SSR (navbar consistent across landing/search/sell/neighbourhoods; rent filter gone). BUG-06 OData filter verified against the **live DDF feed** (`House OR Apartment` → HTTP 200, 92,282 rows; `any()` lambda supported — one transient 500 on retry only). BUG-01/03/06 full end-to-end confirmation needs the API running/deployed (FE dev proxies to the remote Railway API, which won't know `structureType` until deployed)._

- [~] **BUG-01** [P1] _(#001 Neighbourhood)_ Live listing not showing in neighbourhood detail's Live Listings — `getListings` matches `Property` by **exact `city` string** only (`city: neighbourhood.city`), so a listing that belongs to the neighbourhood by location but carries a differing/mismatched-case city string never surfaces. Make it location-aware: city match **+ lat/lng radius fallback** off the neighbourhood coords. `M` · _api/src/modules/neighbourhoods/neighbourhoods.service.ts:78-118, src/components/neighbourhood/LiveListings.tsx_
- [x] **BUG-02** [P1] _(#002 Home hero)_ Selecting a city in the hero search auto-navigates to `/search` — `HeroSearchBar.handleSelect` calls `router.push` on suggestion click. Should only fill the input + keep the selection; navigate to `/search` **only** on Discover (submit). `S` · _src/components/landing/HeroSearchBar.tsx:45-52_
- [~] **BUG-03** [P1] _(#003 Search)_ Search map default city hardcoded to Toronto — `searchStore.mapCenter` is Toronto (`-79.38, 43.65`). Default should be **the user's city via geolocation**; fall back to **Vancouver** (not Toronto) when unavailable. `M` · _src/store/searchStore.ts:87, src/app/(main)/search/SearchPageClient.tsx, MapView.tsx_
- [x] **BUG-04** [P1] _(#004 Global)_ Nav bar inconsistent across pages — three navbars in use (`HomeNavbar`, `layout/Navbar`, `DashboardNavbar`) with different links. Consolidate to **one** navbar: centre = **Home · Buy · Sell · Realtor Hub · Neighbourhood** (logged-out, sign-in/sign-up right) / **Home · Buy · Sell · Neighbourhood** (logged-in, Dashboard right). Buy→`/search?listingType=For+Sale`, Neighbourhood→`/neighbourhoods`. Keep a transparent-over-hero variant for the landing page. `M` · _src/components/landing/HomeNavbar.tsx, src/components/layout/Navbar.tsx, src/components/dashboard/DashboardNavbar.tsx, all page/layout consumers_
- [x] **BUG-05** [P1] _(#005 Search)_ Remove the Buy/Rent filter — no rental data yet. Drop `ListingTypeFilter` (For Sale/For Rent chip) from the filter row and pin `listingType` to `For Sale`; hide the For-Rent-only "Rental Preferences" block. `S` · _src/components/search/FilterPanel.tsx:110-145,542-551,590_
- [~] **BUG-06** [P1] _(#006 Search)_ Home Type filter doesn't filter — root cause: FE `propertyType` options don't map to what DDF can filter; `PropertySubType` (the only synced class field) files condos/townhouses all as "Single Family". **Decision (2026-07-06): build the real filter off DDF `StructureType`** (House/Condo/Townhouse/Apartment/Duplex/Mobile). **Architecture note:** search is **DDF-OData-live** (`ddfQuery.searchProperties`/`getMapPins`), not Postgres — so the fix is an **OData collection filter** (`StructureType/any(s:s eq '…')`), *not* a schema migration/re-sync. Add a `structureType` DTO param, thread FE `HomeTypeFilter` → list + map, keep the residential `PropertySubType` guard as the base. ⚠️ **Verify the `any()` lambda against the live DDF feed** — gate the clause so a rejection can't break unfiltered search. `M` · _api/src/modules/ddf-sync/ddf-query.service.ts:77-127, api/src/modules/search/dto/search-query.dto.ts, src/components/search/FilterPanel.tsx:258-300, src/store/searchStore.ts, src/app/(main)/search/SearchPageClient.tsx_

## 🐞 QA Bug Sweep #2 — Web (2026-07-06)
_Second batch of user-reported web bugs from the co-founder QA table (rows #02–#19). All `Web`. Report row numbers preserved as IDs for traceability with the source spreadsheet. Code locations verified this session. Sizes: S ≤0.5d · M 1–2d._

### Home
- [x] **QA-02** [P0] _(#02 Home · Bug)_ Nav label flickers between "Agents Hub" and "Realtor Hub" — first paint / direct HTML fetch renders "Agents Hub"; after a client-side reload the same link reads "Realtor Hub". Hydration mismatch / stale SSR markup between navbars (RH-FE-01 renamed it to "Realtor Hub"). Make the label stable on every load. `S` · _src/components/landing/HomeNavbar.tsx, src/components/layout/Navbar.tsx_
  - **Already fixed in source (commit 318fbbb) — not a live code bug.** The old `HomeNavbar.tsx` (hardcoded "Agents Hub") was already deleted and `page.tsx` migrated to the shared `Navbar` ("Realtor Hub" → `/realtor-hub`). The flicker came from a stale `.next/dev` build cache serving orphaned SSR chunks of the deleted component. Cleared the gitignored `.next/dev` cache; no source change. ⚠️ **Verify on the live deploy** — if vicinus.ca still flickers, it's a Netlify build/cache/deploy-lag issue, not source.
- [x] **QA-03** [P0] _(#03 Home · Missing)_ Hero search has no autocomplete dropdown — typing a city surfaces no matching cities/neighbourhoods/ZIPs; Enter is the only way to search. Add a suggestion dropdown on type (reuse the search autocomplete source). `M` · _src/components/landing/HeroSearchBar.tsx_
  - **Already fixed in source (commit 318fbbb).** Current `HeroSearchBar.tsx` already implements a full debounced autocomplete dropdown (keyboard nav, outside-click, `handleSelect` fills input) using the same `getAutocomplete` (`GET /search/autocomplete`) source as `SearchBar.tsx`. QA saw the pre-sweep stale build. No source change needed; verify on fresh build/live deploy.

### Search
- [x] **QA-04** [P0] _(#04 Search · Bug)_ Listings show `$0` price and `0 sqft` on card and map pin despite address/beds/baths populated (e.g. 903-70 Temperance St). Reflect real DDF/MLS price & sqft, or hide when genuinely unavailable — never render `$0` / `0 sqft`. Likely a data-mapping/fallback gap shared with QA-08/QA-10. `M` · _src/components/search/SearchResultCard.tsx, CuratorChoiceCard.tsx, MapListingPopup.tsx, PricePin.tsx, src/lib/api (search mapping)_
  - **Done 2026-07-06.** Shared root cause: BE sends `null` when DDF omits a field, FE maps `null → 0` and renders the literal `0`. Fix: price `≤ 0` → "Price on request", sqft `0` → segment hidden. `SearchResultCard.tsx`, `PricePin.tsx` ("Ask" not `$0`), `MapView.tsx` (pass `m.price`, drop `?? 0`), `CuratorChoiceCard.tsx`. FE type-check clean.
- [x] **QA-05** [P0] _(#05 Search · Bug)_ "View Featured" cards render a grey "PHOTO" placeholder instead of a real listing image (reproduced across searches/price filters). Load the real listing photo like standard cards. `S` · _src/components/search/CuratorChoiceCard.tsx_
  - **Done 2026-07-06.** `CuratorChoiceCard` used `<Image src={imageUrl}>` with no fallback (empty src → dark bg shows through). Added the same `FALLBACK_IMAGE` + `imageUrl || FALLBACK_IMAGE` guard `SearchResultCard` uses.
- [x] **QA-07** [P0] _(#07 Search · Bug)_ Clicking a search result intermittently fails to open — navigates to `/properties/[id]` but the page errors ("This page couldn't load / Reload to try again"); a manual reload then renders it. Fix the navigation/SSR-fetch race so it reliably renders first time. `M` · _src/app/(main)/properties/[id]/page.tsx, src/components/search/ResultsList.tsx / SearchResultCard.tsx_
  - **Done 2026-07-06.** SSR `getPropertyDetail` fetch to the remote Railway API had no timeout/retry (cold instance → intermittent first-render fail) and there was **no error boundary** anywhere. Fix: `getPropertyDetail` now uses `AbortSignal.timeout(8000)` + one retry on transient/5xx (404 still → not-found); added `src/app/(main)/properties/[id]/error.tsx` using the Next.js 16 `unstable_retry` prop (re-fetches the RSC = what a manual reload did). _src/lib/api/properties.ts, src/app/(main)/properties/[id]/error.tsx_

### Property Detail
- [x] **QA-08** [P0] _(#08 Property Detail · Bug)_ Always shows `0 sqft` in the summary bar despite full interior/room data (e.g. 87 Craiglee Dr). Show the real sqft or hide when absent. Shares root cause with QA-04. `S` · _src/components/property/PropertyStats.tsx, PropertySummary.tsx, src/lib/api/properties.ts_
  - **Done 2026-07-06.** Same `null → 0` root cause. `PropertyStats.tsx`: Size stat only renders when `sqft > 0`; Price shows "Price on request" instead of `$0`.
- [x] **QA-09** [P0] _(#09 Property Detail · Bug)_ Agent/brokerage missing — agent card shows generic "REALTOR®" with blank avatar/name and "Listing provided by -" is blank, even though the photo caption names the brokerage ("RE/MAX Rouge River Realty Ltd."). Populate agent name + brokerage from the listing data everywhere they're referenced. `M` · _src/components/property/AgentCard.tsx, src/app/(main)/properties/[id]/page.tsx, src/lib/api/properties.ts_
  - **Done 2026-07-06 — ⚠️ backend part needs a Railway deploy.** FE (immediate): `AgentCard.tsx` falls back agent→brokerage→"Listing Brokerage", drops the duplicate blank line; footer joins non-empty agent/brokerage instead of "- · -". API (needs deploy): `ddf-query.service.ts` added `resolveAgentName` (→ `ListAgentFirstName`+`LastName`) and `resolveOfficeName` (→ brokerage-looking `Media[].ShortDescription/LongDescription` caption), applied to `mapProperty` + nearby-open-house cards. API type-check clean. _src/components/property/AgentCard.tsx, src/app/(main)/properties/[id]/page.tsx, api/src/modules/ddf-sync/ddf-query.service.ts_
- [x] **QA-10** [P0] _(#10 Property Detail · Bug)_ Market Context stats (Price/sqft, Price Position, Demand) show `—` placeholders. Compute & display real values when price/sqft are present — largely cascades from QA-08. `S` · _src/components/property/MarketContext.tsx_
  - **Done 2026-07-06 (via QA-08).** `MarketContext` already computes `pricePerSqft` from `price/sqft` when `sqft > 0` — no code change needed; it populates once real sqft flows through (QA-08). Verified.

### Feed
- [x] **QA-16** [P1] _(#16 Feed · Bug)_ Feed never loads media — every card shows a solid black area with an infinite spinner instead of the listing photo/video; text (price/address/bed/bath) loads fine. Load images/video like the Search screen. High impact: whole feed is visually empty. `M` · _src/components/feed/FeedCard.tsx, next.config.ts (image domains)_
  - **Done 2026-07-06.** Root cause: `mapListing()` in `feed/page.tsx` filtered images by a strict file-extension regex, which strips real CREA DDF media (many photo URLs have no extension) → `images: []`; Search never applied that filter, hence it worked. Fixed: relaxed the feed filter to keep any `http(s)` media URL (matching Search), and added `FALLBACK_IMAGE` + `onError`/`videoFailed` handling to `FeedCard` so the spinner/black state resolves on success and failure. Not a `next.config` issue (`unoptimized: true`, plain `<img>`). FE type-check clean. _src/app/(feed)/feed/page.tsx, src/components/feed/FeedCard.tsx_

### Sell / Valuation
- [x] **QA-17** [P1] _(#17 Sell · Missing)_ Intro address field has no autocomplete — typing an address shows no suggestion dropdown despite Google Places being in the stack. Add address autocomplete. `M` · _src/components/sell/SellIntro.tsx_
  - **Done 2026-07-06.** No Google Places in the stack — project uses **Mapbox** (`NEXT_PUBLIC_MAPBOX_TOKEN`, `src/lib/geocode.ts`); internal `getAutocomplete` only returns cities/neighbourhoods, not street addresses. Added `searchAddresses()` (Mapbox forward-geocode, `types=address&country=ca`) + a debounced dropdown in `SellIntro` mirroring `SearchBar.tsx` (keyboard nav, ARIA combobox, outside-click). Reuses existing token, no new key. FE type-check clean. _src/lib/geocode.ts, src/components/sell/SellIntro.tsx_
- [x] **QA-18** [P1] _(#18 Sell · Missing)_ No back navigation mid-wizard — step 1 has "Back to intro" but steps 2 & 3 have no back at all. Allow going back to the previous step from any step. `S` · _src/components/sell/SellWizard.tsx_
  - **Done 2026-07-06.** Added a reusable top-of-step `BackButton` on steps 2 & 3 (calls existing `prev()`), placed under the progress header so it's discoverable above the tall lead form. Step 1's footer "Back to intro" unchanged. _src/components/sell/SellWizard.tsx_
- [x] **QA-19** [P2] _(#19 Sell · Improvement)_ Valuation gated behind contact form with no estimate shown first — the flow asks for name/contact before showing any value. Show a preliminary/range estimate before the lead-capture form (common pattern on competing tools). `M` · _src/components/sell/SellWizard.tsx, SellValuation.tsx_
  - **Done 2026-07-07 — real estimate, no placeholder.** Built lead-free `POST /sell/preview` (`SellPreviewDto`: address + pre-lead wizard answers, no PII) returning `{ low, high, currency:'CAD' }`. Backend refactor: new `previewEstimate()` reuses the existing `generateValuation()` (Gemini) and persists **nothing**, deriving a confidence band (`low×0.97 … high×1.03`) around the genuine estimate; `createValuation()` + the precise/comparables result stay gated behind `POST /sell/valuation`. FE: `getSellPreview()` client + `PreliminaryEstimate` now fetches the real range on step 3 (loading/ready/error; on failure the block hides — no fabricated fallback). Fabricated hash logic fully removed. FE + API type-check clean. ⚠️ Needs the API deploy to return data (FE degrades gracefully until then). _api/src/modules/sell/{sell.service.ts,sell.controller.ts,dto/sell-preview.dto.ts}, src/lib/api/sell.ts, src/components/sell/SellWizard.tsx_

## 🧭 Consumer Onboarding — re-prompt cadence + modal (2026-07-07)
_Show the consumer onboarding flow on sign-up, then re-surface it **every 5th login-session** (not per page-load, not per N days) until the user completes it, as a **dismissible modal on whatever screen they're on** (not a full-page redirect). Decisions confirmed 2026-07-07: (1) granularity = **login-session**; (2) presentation = **modal**. Sizes: S ≤0.5d · M 1–2d._

_**Data storage (for reference):** onboarding answers are persisted server-side on the `User` row as `onboardingData Json?` (`api/prisma/schema.prisma:13`), merged incrementally per step by `updateOnboarding` (`api/src/modules/users/users.service.ts:306-317`) via `PATCH /users/me/onboarding`; the final step sets `onboardingCompleted: true`. Related `User` fields: `loginCount Int`, `onboardingCompleted Boolean`. No client-side storage. This ticket does not change what/where answers are saved — only when/how the flow is shown._

- [x] **ONB-01** [P1] BE — count real login-sessions, not page-loads, and gate the prompt on every 5th.
  - **Done 2026-07-07.** `sessionId` sourced **server-side** from the Clerk JWT `sid` claim (`ClerkAuthGuard` now stashes `request.sessionId = payload.sid`; new `@CurrentSessionId()` decorator; threaded into `ping`) — can't be spoofed, no FE plumbing needed. `ping()` increments `loginCount` (now a session counter) + writes `lastSessionId` **only** when the incoming `sid` differs from the stored one (null = new). Cadence: `showOnboarding = !onboardingCompleted && loginCount % 5 === 1` (sessions 1, 6, 11…). Added `User.lastSessionId String?` + migration `20260707195438_add_last_session_id`. FE + API type-check clean; `prisma generate` OK. ⚠️ Migration auto-applies via Railway `preDeployCommand: prisma migrate deploy`. _api/src/common/guards/clerk-auth.guard.ts, api/src/common/decorators/current-session.decorator.ts (new), api/src/modules/users/users.{controller,service}.ts, api/prisma/schema.prisma + migration_
- [x] **ONB-02** [P1] FE — show onboarding as a dismissible modal over the current screen (not a redirect).
  - **Done 2026-07-07.** New `onboardingStore` (Zustand, `{isOpen,open,close}`) + `OnboardingModal` (accessible overlay: role=dialog/aria-modal, backdrop/Esc/X dismiss, scroll-lock, focus trap+restore, capped to `sm:h-[90vh]`). `OnboardingGate` now `open()`s the modal instead of `router.push('/onboarding')`, guards double-open, still fires globally. Mounted `<OnboardingGate/>`+`<OnboardingModal/>` in root `layout.tsx` — **note: `OnboardingGate` was never actually mounted before**, so onboarding was previously driven only by a server-side ping+redirect on the dashboard page, which is now **removed** (`(dashboard)/dashboard/page.tsx`) so the global gate owns it everywhere. Complete → persists `completed:true` + closes; skip/backdrop/Esc → closes without completing (re-prompts next cadence). `/onboarding` deep-link untouched. Stale "odd login" comment fixed. _src/store/onboardingStore.ts (new), src/components/onboarding/OnboardingModal.tsx (new), OnboardingGate.tsx, src/app/layout.tsx, src/app/(dashboard)/dashboard/page.tsx_

## 🔎 Co-Founder Product Review — QA verification pass (2026-07-06)
_Screen-by-screen verification of the live site from the co-founder review (`Vicinus_Combined_Review_and_Pipeline_Plan.docx`, Part A). These are **verification passes**, not builds — walk each screen on phone + desktop, run the test cases, and log Bugs/Improvements/Missing/Remove. P0/P1 assigned here as PE agent (matches the doc's Master Prioritized Backlog). Some items overlap the QA-Audit backlog below (code-level findings) — cross-reference rather than duplicate._

- [ ] **REV-01** [P0] Home / Landing — hero search → `/search` (autocomplete + Enter), featured cards load & click through, navbar + footer links, images, <3s first load. `S` · _vicinus.ca/_
- [ ] **REV-02** [P0] Search — city fly-to, skeleton → real cards <2s, map pins <2s + popups, price/beds/type/for-rent filters, More Filters, list/map/both views, pagination, save-search, autocomplete, clear-resets. `M` · _vicinus.ca/search_
- [ ] **REV-03** [P0] Property Detail — photo gallery scroll, price/beds/baths/sqft/address, location map, agent card, save/share, open houses, tour/video, mortgage calc, MLS #, back-nav, facts accuracy, <3s load. `M` · _vicinus.ca/properties/[id]_
- [ ] **REV-04** [P0] Auth (Sign In + Sign Up) — login/signup succeed, email verification arrives & completes, wrong-password + duplicate-email errors, forgot-password reset e2e, Google (if shown), redirect to `/dashboard`. `S` · _vicinus.ca/sign-in, /sign-up_
- [ ] **REV-05** [P0] Onboarding — wizard advances/back/skip, progress indicator correct, completes → `/dashboard`, answers affect dashboard, resume-on-refresh, mobile fit. `S` · _vicinus.ca/onboarding_
- [ ] **REV-06** [P1] Feed — first card loads, scroll-snap, images, price/beds/baths, video indicator + playback, filter bar (city/sale-rent/price), infinite scroll, card → detail, portrait/expand, end state. `M` · _vicinus.ca/feed_
- [ ] **REV-07** [P1] Neighbourhoods (grid + detail) — real Canadian data (no placeholders), card → detail, AI summaries (Safety/Daily Life/Schools/Growth), real walk/transit scores, listings, essentials w/ distances, agents, map. `M` · _vicinus.ca/neighbourhoods, /neighbourhoods/[slug]_
- [ ] **REV-08** [P1] Dashboard — loads signed-in, welcome name, saved properties, recently-visited (real), recent searches, featured (not blank), notifications, remove-saved, sign out. `S` · _vicinus.ca/dashboard_
- [ ] **REV-09** [P1] Sell / Valuation — intro → wizard, steps advance/back, processing state, real estimate (not $0), API error state, next-step CTA. `S` · _vicinus.ca/sell_
- [ ] **REV-10** [P1] Mobile responsiveness sweep — hero/nav/forms/map-list/feed/cards stack cleanly on phone across all 11 screens; 44px tap targets. `M` · _all screens_
- [ ] **REV-11** [P1] Latency verification — search list & map pins <2s, property detail <3s, feed first card, neighbourhood AI summary; record actuals. `S` · _perf pass_
- [ ] **REV-12** [P2] Remove premature/clutter — "Coming Soon" home block, empty Intelligence Panel, placeholder neighbourhood agents, empty Assessment History; hide-when-empty. `S` · _multiple_
- [ ] **REV-13** [P2] Missing niceties — sort on search, save-from-feed card, valuation confidence range, neighbourhood search/filter. `M` · _multiple_

## 🚀 Realtor Hub — coming-soon landing + waitlist (2026-07-03)
_Rename the home navbar "Agents Hub" item to "Realtor Hub" and repoint it from `/search` to a new professionals coming-soon page (the "Vicinus is coming for real estate professionals" design). Page is a marketing landing with hero, 3 feature cards (Augmented Neighbourhood Data · Neighbourhood Bidding · Short-Form Listing Content), a Founding Member band, and a "Join the Waitlist" form (Full Name · Professional Email · Brokerage · City/Market) that POSTs to a new lead-capture endpoint. Backend mirrors the existing `sell` module / `SellerLead` pattern. Route: `/realtor-hub`. Sizes: S ≤0.5d · M 1–2d._

### Frontend
- [x] **RH-FE-01** [P1] Rename nav item "Agents Hub" → "Realtor Hub" and repoint `href` `/search` → `/realtor-hub` — update both the desktop link and the mobile-menu array entry. `S` · _src/components/landing/HomeNavbar.tsx (lines 51–53 desktop, 109 mobile)_
- [x] **RH-FE-02** [P1] Scaffold `/realtor-hub` route + page shell — new App Router segment rendering the section components below; reuse `HomeNavbar` + footer; server component wrapper, client only where the form needs it. `S` · deps: RH-FE-01 · _src/app/realtor-hub/page.tsx_
- [x] **RH-FE-03** [P1] Hero section — dark-green band, headline "Vicinus is coming for real estate professionals.", subcopy, "Get Early Access" CTA (scrolls to waitlist form), and the product-preview mockup with the "PREDICTIVE HEAT +24% Intent" badge. `M` · deps: RH-FE-02 · _src/components/realtor-hub/RealtorHubHero.tsx_
- [x] **RH-FE-04** [P1] Feature cards section — "Designed for those who curate the market." heading + 3 cards (Augmented Neighbourhood Data · Neighbourhood Bidding · Short-Form Listing Content) with image, title, body; responsive 1→3 column grid. `M` · deps: RH-FE-02 · _src/components/realtor-hub/RealtorHubFeatures.tsx_
- [x] **RH-FE-05** [P2] Founding Member band — dark card "Join as a Founding Member." + copy + "Limited Access" CTA (scrolls to form). `S` · deps: RH-FE-02 · _src/components/realtor-hub/FoundingMemberBand.tsx_
- [x] **RH-FE-06** [P1] Waitlist form (client) — Full Name · Professional Email · Brokerage · City/Market; client-side validation (required + email), submit → RH-FE-07, loading/success/error states, disable-on-submit, honeypot field. `M` · deps: RH-FE-02, RH-FE-07 · _src/components/realtor-hub/WaitlistForm.tsx_
- [x] **RH-FE-07** [P1] API client `submitRealtorWaitlist()` — typed fetch wrapper POSTing to the BE endpoint (RH-BE-02), matching the existing `src/lib/api/*` pattern. `S` · deps: RH-BE-02 · _src/lib/api/waitlist.ts_
- [x] **RH-FE-08** [P2] Responsive + design-token polish — mobile/tablet layout, brand colours (`#1C3829` etc.), 44px tap targets, focus states; screenshot-verify against the design. `S` · deps: RH-FE-03…06 · _src/components/realtor-hub/*_
- [x] **RH-FE-09** [P2] Emit `realtor_waitlist_submitted` analytics event on success — ✅ guarded no-op hook in place (`window.posthog?.capture(...)` in a try/catch); becomes live automatically once DATA-08 mounts posthog. `S` · deps: RH-FE-06, DATA-08 · _src/components/realtor-hub/WaitlistForm.tsx_

### Backend
- [x] **RH-BE-01** [P1] `RealtorWaitlist` Prisma model + migration — fields: `id`, `fullName`, `email`, `brokerage?`, `cityMarket?`, `source?`, `createdAt`; unique index on `email` for dedupe (mirror `SellerLead`). `S` · _api/prisma/schema.prisma_
- [x] **RH-BE-02** [P1] `WaitlistModule` + `POST /waitlist/realtor` — controller + DTO with `class-validator` (`@IsString`, `@IsEmail`, `@MinLength`), Swagger `@ApiTags/@ApiOperation`; wire module into `app.module.ts` (mirror `sell` module). `M` · deps: RH-BE-01 · _api/src/modules/waitlist/*_
- [x] **RH-BE-03** [P1] `WaitlistService.join()` — persist lead, idempotent upsert on `email` (re-submit returns 200 without dup), return `{ ok: true }`; no PII in logs. `S` · deps: RH-BE-02 · _api/src/modules/waitlist/waitlist.service.ts_
- [~] **RH-BE-04** [P2] Spam / abuse protection — ✅ honeypot done (service silently drops when `company` filled). ⏳ Rate limit deferred: `@nestjs/throttler` isn't installed yet — left as a TODO consistent with the existing `main.ts` throttler note; wire on RH-BE-04b / DATA-06. `S` · deps: RH-BE-02 · _api/src/modules/waitlist/*, api/src/app.module.ts_
- [ ] **RH-BE-05** [P2] Server-side `realtor_waitlist_submitted` conversion event + optional ops notification (email/Slack) on new signup — follow the DATA-11 server-event pattern. Deferred: blocked on the (unbuilt) analytics pipeline; a labelled TODO + `logger.log` hook is in place in the service. `S` · deps: RH-BE-03, DATA-07 · _api/src/modules/waitlist/waitlist.service.ts_

## 📊 Data Pipeline — analytics + personalization (2026-07-01)
_Stand up a product-analytics + personalization data pipeline. Full plan: `docs/data-pipeline-plan.md`. **Proposal — nothing built; do not implement until approved.** Recommended stack: PostHog (EU Cloud) as primary product-analytics/dashboard tool + an owned `AnalyticsEvent` Postgres table; server-side capture for conversion events; deterministic rules-based feed ranker in v1. **DATA-01 is a decision gate — several tasks are blocked on it.** Sizes: S ≤0.5d · M 1–2d · L 3–5d._

### Phase 0 — Foundations & Consent
- [x] **DATA-01** [P0] Decide analytics vendor & data-residency — ✅ **DECIDED 2026-07-01: PostHog EU Cloud.** No existing analytics stack; EU region for PIPEDA-defensibility. Budget ceiling + session-replay on/off still TBD. `S` · _decision/infra_
- [ ] **DATA-02** [P1] Consent banner + store (PIPEDA) — accept/reject/manage, first-party cookie, Zustand store; gates all non-essential tracking. `M` · deps: DATA-01 · _src/components/consent/, src/store/_
- [ ] **DATA-03** [P1] Install & init posthog-js behind consent — `opt_out_capturing_by_default`, `identify(clerkId)` on sign-in, mint `anonymous_id`. `M` · deps: DATA-02 · _src/lib/analytics/, src/app/layout.tsx, providers_
- [ ] **DATA-04** [P1] AnalyticsModule + `POST /events` — public endpoint, opportunistic Clerk-token read for `user_id`, DTO validation. `M` · deps: DATA-01 · _api/src/modules/analytics/_
- [ ] **DATA-05** [P1] `AnalyticsEvent` Prisma model + migration — append-only, indexes per plan §7.2. `S` · deps: DATA-04 · _api/prisma/schema.prisma_
- [ ] **DATA-06** [P1] Install & wire `@nestjs/throttler` — tight rate limit on `/events` (closes main.ts TODO). `S` · deps: DATA-04 · _api/src/main.ts, app.module.ts_
- [ ] **DATA-07** [P1] posthog-node in API + `AnalyticsService.track()` — dual-write Postgres + PostHog. `M` · deps: DATA-04, DATA-05 · _api/src/modules/analytics/_

### Phase 1 — Taxonomy & Conversion
- [ ] **DATA-08** [P1] Typed `track()` wrapper + event-name enum — single source of truth for event schema (plan §2). `M` · deps: DATA-03 · _src/lib/analytics/_
- [ ] **DATA-09** [P1] Instrument lifecycle/page/search/feed/property events (client) — full §2 catalog. `L` · deps: DATA-08 · _src/app/**, src/components/feed/*, property/*_
- [ ] **DATA-10** [P1] Onboarding per-step events — started/step/skip/complete/abandon. `M` · deps: DATA-08 · _src/components/onboarding/OnboardingWizard.tsx_
- [ ] **DATA-11** [P1] Server-side conversion events — signed_up, property_saved, search_performed, seller_lead_submitted. `M` · deps: DATA-07 · _api/.../users.service.ts, sell.service.ts, search.service.ts_
- [ ] **DATA-12** [P1] Link `SellerLead` → `User` — nullable `userId` FK + attribute when authed. `S` · deps: DATA-05 · _api/prisma/schema.prisma, sell.*_
- [ ] **DATA-13** [P1] Forward `anonymous_id` on public search — header client→`search_performed`. `S` · deps: DATA-08 · _src/lib/api/*, api/.../search.controller.ts_
- [ ] **DATA-14** [P1] Build Growth / Conversion / Onboarding dashboards — insights + funnels + cohorts in PostHog. `M` · deps: events flowing · _PostHog config_

### Phase 2 — Profile & Personalized Feed
- [ ] **DATA-15** → **superseded by BRIEF-01** (2026-07-21). Pulled forward out of Phase 2: the profile only needs `User.onboardingData`, not the event pipeline, so the `deps: Phase 1` gate was unnecessary. Build it there, not here.
- [ ] **DATA-16** → **superseded by BRIEF-02** (2026-07-21).
- [ ] **DATA-17** → **superseded by BRIEF-03** (2026-07-21).
- [ ] **DATA-18** [P1] `FeedModule GET /feed` with rules-based ranker — scoring per §7.3, weights via flag/config, anon fallback. `L` · deps: BRIEF-02 (was DATA-16) · _api/src/modules/feed/_
- [ ] **DATA-19** [P1] FE feed consumes `/feed` + emits impression/click + `feed_variant` — replace direct searchProperties, reuse IntersectionObserver. `M` · deps: DATA-18 · _src/app/(feed)/feed/page.tsx, src/lib/api/_
- [ ] **DATA-20** [P2] `feed_ranking_v1` flag + A/B on engagement rate. `S` · deps: DATA-19 · _PostHog flag + dashboard_

### Phase 3 — Ops & Scale
- [ ] **DATA-21** [P1] `/health` + `/health/deep` endpoints — Postgres + Redis checks. `S` · _api/src/_
- [ ] **DATA-22** [P2] Sentry APM (errors + perf) + uptime monitor — DDF live-path latency, Web Vitals. `M` · _FE + API + infra_
- [ ] **DATA-23** [P2] `EventIngestLog` + ingestion-lag alerting — mirror DdfSyncLog pattern. `S` · deps: DATA-04 · _api_
- [ ] **DATA-24** [P1] Retention prune cron + erasure hook — 13-mo prune; delete-on-account-deletion (Postgres + PostHog). `M` · deps: DATA-05 · _api @nestjs/schedule_
- [ ] **DATA-25** [P2] (Optional) Metabase companion on Railway — SQL dashboards over AnalyticsEvent. `M` · deps: DATA-05 · _infra_

### Phase 4 — AI / Learned Reranker (v2 · next release)
_The **AI effort** broken out from the plan (§7.4 / §11 of `docs/data-pipeline-plan.md` + combined doc). Deferred until v1 (deterministic, DATA-15…20) is live and ≥3–6 months of `AnalyticsEvent` history exist — the owned event log is the training substrate. **Total AI effort ≈ 15–25 eng-days (XL).** All P2 (post-v1)._
- [ ] **AI-01** [P2] Prereq gate — accumulate ≥3–6 mo `AnalyticsEvent` history + a stable v1 deterministic **engagement-rate baseline** for the learned model to beat; go/no-go. _Gating milestone (not eng-days)._ · deps: DATA-18, DATA-19, DATA-20 · _decision gate_
- [ ] **AI-02** [P2] Feature & embedding pipeline — listing2vec + user vectors from view/save/impression sequences in `AnalyticsEvent`; offline feature store. `L` _(3–5d)_ · deps: AI-01 · _api analytics + offline job_
- [ ] **AI-03** [P2] Offline training + eval harness — train candidate learned reranker; replay/holdout eval vs the v1 ranker on the engagement proxy; model selection. `L` _(3–5d)_ · deps: AI-02 · _ml/offline_
- [ ] **AI-04** [P2] Online serving — serve the learned reranker in `FeedModule` behind a `feed_ranking_v2` flag; shadow-mode → A/B vs v1; hold a p95 latency budget. `L` _(3–5d)_ · deps: AI-03, DATA-18 · _api/src/modules/feed/_
- [ ] **AI-05** [P2] Guardrails & monitoring — preserve CREA/DDF attribution on ranked cards, explainable/deterministic fallback, drift + ranking-quality monitoring, one-flag rollback. `M` _(1–2d)_ · deps: AI-04 · _api + PostHog_

## 🏘️ Neighbourhoods — 2-level filter redesign (2026-06-29)
_Province → City two-level filter with context-aware pre-selection, adaptive featured section, and mobile scroll. Based on product spec + UI design approved this session._

- [x] **NBR-01** [P1] Replace region pills with 2-level Province → City filter — refactor `NeighbourhoodsClient.tsx` to derive Level 1 province pills and Level 2 city pills from `Neighbourhood.city` data; selecting a province resets city; Level 2 hidden on "All Canada"; Level 2 skipped when province has only 1 city. — _src/components/neighbourhood/NeighbourhoodsClient.tsx, src/lib/neighbourhood-regions.ts_
- [x] **NBR-02** [P1] Default-select BC (most-populated province) on page load — when only one province exists pre-select it; when multiple provinces exist default to "All Canada". — _src/components/neighbourhood/NeighbourhoodsClient.tsx_
- [x] **NBR-03** [P1] Context-aware filter pre-selection from search store — on mount read `searchStore.query` / `searchStore.userCity`; if it matches a known `Neighbourhood.city` auto pre-select province + city pills; fall back to default if unrecognised. — _src/components/neighbourhood/NeighbourhoodsClient.tsx_
- [x] **NBR-04** [P1] Adaptive featured section label + content per filter state — "Editor's Picks" (All Canada) · "[Province] Highlights" (province only) · "[City]" (city selected or context-aware); hide section when fewer than 2 neighbourhoods match. — _src/components/neighbourhood/NeighbourhoodsClient.tsx_
- [x] **NBR-05** [P2] Hide city/region tag on grid cards when a specific city is selected — tag is redundant when every card is already scoped to that city; show tag on All Canada + province-level views. — _src/components/neighbourhood/NeighbourhoodsClient.tsx_
- [x] **NBR-06** [P2] Empty state when selected city has no neighbourhoods — dashed-border box with heading "No neighbourhoods in [City] yet", subtext, and two CTA pills ("Browse all [Province]" + "See nearby cities"); hide grid and featured. — _src/components/neighbourhood/NeighbourhoodsClient.tsx_
- [x] **NBR-07** [P2] Sticky filter bar with blur backdrop — `position: sticky; top: 0`; `background: rgba(250,249,246,0.94)`; `backdrop-filter: blur(8px)`; 1px #E8E6E1 bottom border; pills compress to 30px in sticky state. — _src/components/neighbourhood/NeighbourhoodsClient.tsx_
- [x] **NBR-08** [P2] Mobile horizontal-scroll pill rows with fade mask — `overflow-x: auto`; no scrollbar; 24px right-edge fade mask; total sticky area ≤88px on mobile; Level 2 animates in with `max-height 0→40px` + `opacity 0→1` at 160ms ease-out. — _src/components/neighbourhood/NeighbourhoodsClient.tsx_

## 🏘️ Neighbourhoods — BC scale-up + search (2026-06-30)
_Scale BC to 382 neighbourhoods across 157 cities. Structure stays **Province → City → Neighbourhood** (regional-district tier explicitly dropped). The city-pill tier must handle 157 cities: collapsed by default (top cities by count), expandable to a full A–Z list. Also seed the full BC dataset so names surface in the `/search/autocomplete` dropdown. Extends NBR-01…08 — do NOT rebuild the sticky bar, featured/spotlight, count badges, context-aware pre-selection, empty states, or mobile scroll/fade already built._

- [x] **NBR-09** [P1] Rewrite `bc-neighbourhoods.ts` seed to load the full CSV (382 neighbourhoods / 157 cities) — replace the hardcoded municipality arrays with a parse of `bc_municipalities_neighbourhoods.csv` (cols: neighbourhood, municipality, type, regional_district). Seed one `Neighbourhood` row per CSV line: `name` = neighbourhood, `city` = municipality, `province` = 'BC'; keep idempotent slug upsert + optional Mapbox geocode. Cities with a single neighbourhood should still produce a browsable row. Confirm the CSV path (currently `~/Downloads/…`) is moved into the repo (e.g. `api/prisma/seeds/data/`) so the seed is reproducible. — _api/prisma/seeds/bc-neighbourhoods.ts, api/prisma/seeds/data/bc_municipalities_neighbourhoods.csv_
- [x] **NBR-10** [P1] Run the BC seed and verify data flows into the index — execute the NBR-09 seed against the dev DB; confirm `getNeighbourhoods()` returns the 382 rows and the index page renders them (no MOCK fallback). Blocker check: seed must run (needs `DATABASE_URL`; Mapbox token optional — coords null is acceptable). — _src/lib/api/neighbourhoods.ts, src/app/(main)/neighbourhoods/page.tsx_
- [x] **NBR-11** [P1] Drop the regional-district (Level 2) tier from `NeighbourhoodsClient.tsx` — remove `districtOptions`/`selectedDistrict`/`showDistrictRow` and the district PillRow so the filter is a clean two-level Province → City per the agreed design. `districtForCity`/`DISTRICT_MUNICIPALITIES` in `neighbourhood-regions.ts` become dead once unused — remove or leave a note. Ripple through `deriveFilters`, `filterNeighbourhoods`, `pickFeatured`, `buildFeaturedLabel`, and the empty-state CTAs. — _src/components/neighbourhood/NeighbourhoodsClient.tsx, src/lib/neighbourhood-regions.ts_
- [x] **NBR-12** [P1] Replace the municipality combobox with a collapsed city-pill tier — swap `MunicipalityCombobox` for a pill row showing top cities ranked by neighbourhood count, gated to cities with **≥2 neighbourhoods** (adjustable threshold — expose as a named const, e.g. `MIN_CITY_PILL_COUNT = 2`, and note it can be tuned). Keep the "All British Columbia" pill (existing all-cities pill) and reuse the existing `Pill`, `PillRow`, count badges, sticky compaction, and mobile fade mask. — _src/components/neighbourhood/NeighbourhoodsClient.tsx_
- [x] **NBR-13** [P1] Add "Show all 157 ›" expand / "Collapse" behaviour for the city tier — a trailing affordance after the collapsed pills reveals the full **alphabetical (A–Z)** list of every city as pills; a "Collapse" control returns to the NBR-12 default. No search box (explicitly decided against). Manage expanded/collapsed via local state; count in the label should reflect the actual city total. — _src/components/neighbourhood/NeighbourhoodsClient.tsx_
- [ ] **NBR-14** [P2] Optional: A–Z section headings in the expanded city list — group the full expanded pill list under letter headers (A, B, C…) for scannability. Sub-task of NBR-13; skip if it complicates the mobile horizontal-scroll row. — _src/components/neighbourhood/NeighbourhoodsClient.tsx_
- [x] **NBR-15** [P1] Verify BC city + neighbourhood names surface in the search dropdown — after NBR-10, confirm `/search/autocomplete` returns both cities (rows where `name === city`) and sub-area neighbourhoods (rows where `name !== city`) for BC queries, and that `SearchBar` + `HeroSearchBar` render them (city vs neighbourhood icon/type). No FE or BE-endpoint change expected — the endpoint is already DB-driven and partitions city vs neighbourhood; this is verification of the NBR-09/10 seed. — _api/src/modules/search/search.service.ts (read-only verify), src/components/search/SearchBar.tsx, src/components/landing/HeroSearchBar.tsx_

## 🛠️ Backend — new endpoints (2026-06-19)
_New BE work to replace mock/static data (see mock-data audit). "Wiring-only" items excluded — those have endpoints already._

- [x] **BE-A** [P1] `GET /search/autocomplete?q=` — cities (static CANADIAN_CITIES) + neighbourhoods (seeded `Neighbourhood` table via Prisma), prefix-ranked, deduped (drops municipality rows that duplicate a city), Redis-cached. FE `SearchBar` + `HeroSearchBar` now call it (debounced, race-safe) instead of `MOCK_AUTOCOMPLETE`. — _api/src/modules/search/search.service.ts, src/components/search/SearchBar.tsx, src/components/landing/HeroSearchBar.tsx_
- [ ] **BE-B** [P1] `GET /users/me/notifications` — alerts (saved-search price drops) + schedule (saved listings' open houses). Replaces dashboard IntelligencePanel/NotificationsPanel mocks. Scope to alerts+schedule; drop fake "Sarah" message until real messaging exists.
- [ ] **BE-C** [P2] `POST/DELETE /properties/:id/like` + like count in feed payload. New `PropertyLike` model. Replaces `Math.random()` like count. (Or defer + remove metric.)
- [ ] **BE-D** [P1] `POST /sell/valuation` ✅ built — needs e2e test + real DDF comps instead of Gemini-fabricated.
- [ ] **BE-E** [P2] Populate `Neighbourhood.walkScore/transitScore` (replaces `?? 80 / ?? 8`).
- [ ] **BE-F** [P2] Add `Neighbourhood.flavors` field + seed (replaces hardcoded `NeighbourhoodFlavors`).
- [x] **BE-G** [P1] Real market context wired to property detail. BE `getMarketContext` now resolves subject by `ddfListingKey` (was UUID-only → why it was never wired); returns median price/$ per sqft/DOM, price position, demand from live active comps. FE detail page fetches it server-side; `MarketContext` shows real area medians + "Price Position" (dropped the impossible YoY +8.4% — DDF has no historical/sold data) and the fabricated $1,290/18d subs. Verified via SSR HTML. — _api/.../properties.service.ts, src/lib/api/properties.ts, src/components/property/MarketContext.tsx, properties/[id]/page.tsx_
- [x] **BE-H** [P2] `GET /properties/featured` — curator picks if flagged, else top-priced active BC residential (beds≥3, sqft>1000, has photos). Landing page "Curated Highlights" now shows real DDF listings (Whistler/White Rock/Vancouver) instead of fabricated US homes (Palm Springs/Aspen/Chelsea). Section hides when empty. Verified via live HTML. — _api/.../properties.{controller,service}.ts, src/lib/api/properties.ts, src/app/page.tsx_

## 🏷️ Sell Flow — new feature (2026-06-18)
_Navbar "Sell" link already points to `/sell` (route doesn't exist yet → 404). Build the seller lead-gen flow there. Unauthenticated lead-capture; reuse existing Vicinus design tokens (#1C3829 / #A3E635 / #FAF9F6), NOT the mockup's "EstateIntelligence"/"The Curator" branding. **Decision: persist lead to BE; valuation generated by Gemini** (reuse AiModule's `@google/generative-ai` pattern + `GEMINI_API_KEY`)._

- [ ] **SELL-01** [P1] Intro screen at `/sell` — hero "What is your property worth?" with address input + "Explore Value" CTA. Address carries into the question flow. — _New: `src/app/sell/page.tsx`, `src/components/sell/SellIntro.tsx`_
- [ ] **SELL-02** [P1] 3-question flow (Step 1/3 selling priority · Step 2/3 biggest hurdle · Step 3/3 advisory preference + name/email/phone lead form), progress bar, back/next, answers threaded across steps. — _New: `src/components/sell/SellWizard.tsx`_
- [ ] **SELL-03** [P1] BE: `SellerLead` Prisma model + migration; `SellModule` (`POST /sell/valuation`) persists the lead and returns a Gemini-generated valuation (value range, confidence, market pulse, comparables). — _New: `api/prisma/schema.prisma`, `api/src/modules/sell/*`, `api/src/app.module.ts`_
- [ ] **SELL-04** [P1] Valuation detail screen — address as title, Gemini value range, confidence, AI market pulse, comparable-sales cards, "Schedule Walkthrough" CTA. — _New: `src/components/sell/SellValuation.tsx`, `src/lib/api/sell.ts`_
- [ ] **SELL-05** [P2] Wire transitions: intro → wizard → valuation as one state-driven client flow (loading state while Gemini generates). — _Files: `src/app/sell/page.tsx`_

## ✅ Completed this session (2026-06-14)
- [x] **FIX-01** [P0] Home hero search now hydrates the search page from URL params (`q`, `priceRange`, etc.). — _Files: `src/app/(main)/search/page.tsx`, `SearchPageClient.tsx`, `src/components/search/SearchBar.tsx`_
- [x] **FIX-02** [P0] Saved/visited properties end-to-end: Clerk token attached to API client (`src/lib/api/client.ts`); `SavedProperty`/`VisitedProperty` now store the DDF ListingKey instead of a Property FK (migration `20260614210251_saved_visited_store_listing_key`); dashboard resolves cards from the local Property table with a live DDF fallback and returns shapes matching the FE types. — _Files: `api/src/modules/users/users.service.ts`_
- [x] **FIX-03** [P1] Search `listingType` (For Sale/For Rent) wired FE→BE; advanced filters with no DDF backing labeled "coming soon". — _Files: `api/.../search-query.dto.ts`, `ddf-query.service.ts`, `SearchPageClient.tsx`, `src/components/search/FilterPanel.tsx`_

## 🔧 Backlog — QA Audit (2026-06-14)
_Enumerated findings: 2 P0 · 14 P1 · 11 P2 = 27. (Audit header stated "3 P0, 14 P1, 12 P2"; only 27 distinct findings were listed.)_

### Landing
- [ ] **QA-01** [P1] Hardcoded US "Curated Highlights" on homepage — _src/app/page.tsx:18-55,284_ — static US luxury homes rendered on a Canadian platform, hrefs to mock /properties/1,2,3.
- [ ] **QA-02** [P1] `Math.random()` for user-facing "Active Editorials" count — _src/app/page.tsx:132-133_ — fabricated number, hydration mismatch risk.
- [ ] **QA-03** [P2] Hero search does not pass listing type — _src/app/page.tsx:172-204_ — no For Sale/Rent selector from homepage.

### Search
- [ ] **QA-04** [P1] Autocomplete is mock-only; API client method dead — _src/components/search/SearchBar.tsx:6,36-50, src/lib/api/search.ts:45-46_ — filters MOCK_AUTOCOMPLETE (SF data); /search/autocomplete endpoint does not exist in BE.
- [ ] **QA-05** [P1] Saved searches never persist to backend — _src/components/search/SaveSearch.tsx:15-22, src/store/searchStore.ts:100-114_ — store-only; saveSearch/getSavedSearches API + /users/me/searches endpoints exist but never called; vanish on refresh, email alerts never fire.
- [ ] **QA-06** [P2] FE→BE saved-search body shape mismatch — _src/lib/api/search.ts:32-37 vs api/src/modules/search/dto/create-saved-search.dto.ts:4-18_ — FE sends name/query/filters/mapBounds; DTO only accepts name+filters; query/mapBounds dropped.
- [ ] **QA-07** [P2] city/province search params defined but never sent — _src/app/(main)/search/SearchPageClient.tsx:89-108, src/lib/api/search.ts:4-24_ — structured city filtering unreachable from UI.
- [ ] **QA-08** [P2] Advanced filters UI-only (known) — _src/components/search/FilterPanel.tsx:345-534_ — basement/stories/days-on-market/open-house/coming-soon/financial/rental controls not sent to API.

### Property Detail
- [ ] **QA-09** [P0] Mock fallback serves San Francisco/USD data as a real listing — _src/app/(main)/properties/[id]/page.tsx:31,61, src/types/property.ts:170-300_ — getMockPropertyDetail returns SF properties for null/unknown ids; users shown fabricated American listings + fake history.
- [ ] **QA-10** [P1] "Send Message"/"Contact Agent" no handler or fallback — _src/components/property/AgentCard.tsx:48-51, src/components/property/ActionBar.tsx:177-183_ — Send Message has no onClick; Contact Agent uses tel:${agentPhone} but agentPhone never populated for live DDF (src/lib/api/properties.ts:64-98), falls back to href="#".
- [ ] **QA-11** [P1] MarketContext renders hardcoded comparables + fabricated YoY — _src/components/property/MarketContext.tsx:11-12,28,34,52_ — priceChange ?? 8.4, "avg 18d", "$1,290" hardcoded; BE /properties/:id/market-context never called.
- [ ] **QA-12** [P1] NeighbourhoodContextScore hardcoded Walk/Lifestyle scores — _src/components/property/NeighbourhoodContextScore.tsx:23-25,33_ — walkScore ?? 80, lifestyleScore ?? 8; same fabricated scores on every live property; "View full profile" href="#".
- [ ] **QA-13** [P2] Mortgage CTAs non-functional — _src/components/property/MortgageAnalysis.tsx:147-152_ — "Get Pre-Approved"/"Connect with Agent" no handlers.

### Dashboard / Saved
- [ ] **QA-14** [P1] Saved-property state never hydrates; save heart wrong on load — _src/store/userStore.ts:6,17, src/components/property/ActionBar.tsx:101-106_ — setSaved has no callers; savedPropertyIds empty on load; heart shows "Save" for already-saved.
- [ ] **QA-15** [P1] IntelligencePanel entirely hardcoded mock — _src/components/dashboard/IntelligencePanel.tsx:11-73,108-112_ — fixed open house, fake message from "Sarah", fake price drop, hardcoded "5" badge; tabs inert.
- [ ] **QA-16** [P1] RecentSearches hardcoded US searches, no handlers — _src/components/dashboard/RecentSearches.tsx:1-7 via WelcomeBanner.tsx:23_ — Chelsea/Aspen/Malibu/Whistler chips, no onClick.
- [ ] **QA-17** [P1] VisitedProperties fabricates status tags/metadata — _src/components/dashboard/VisitedProperties.tsx:28-29,36,69-71,90-97_ — index%2 "OFFER PENDING"/"FAVORITE"; SHORTLIST/DOCS/OFFER buttons no handlers.
- [ ] **QA-18** [P1] "Schedule Tour"/"Add to Calendar" CTAs dead — _src/components/dashboard/SavedProperties.tsx:79-83, src/components/dashboard/FeaturedProperty.tsx:62-66_ — no handlers; FeaturedProperty hardcodes "Next Open House Oct 12".
- [ ] **QA-19** [P2] EditorialCurations tab toggle inert; cards not clickable — _src/components/dashboard/EditorialCurations.tsx:84-85,101-115,119-123_ — both tabs identical; cards have no link despite BE ctaUrl.
- [ ] **QA-20** [P2] Dead mock arrays shipped — _src/components/dashboard/VecinusPanel.tsx:43-56 (unused), EditorialCurations.tsx:9-42 (MOCK_CURATIONS fallback)_.

### Neighbourhoods
- [ ] **QA-21** [P1] Neighbourhood data silently falls back to Rosedale/US mock — _src/lib/api/neighbourhoods.ts:17-64,68-73,137-158_ — MOCK_* datasets on empty API; getNeighbourhood merges Rosedale mock into every slug.
- [ ] **QA-22** [P2] Neighbourhood agents email:null but UI implies email contact — _api/src/modules/neighbourhoods/neighbourhoods.service.ts:160-171,227_.

### AI
- [ ] **QA-23** [P0] AI summary feature fully orphaned — _api/src/modules/ai/ai.controller.ts:12-26, ai.service.ts:55-75; no FE caller_ — /ai/property-summary/:id endpoints exist but nothing calls them; AiService queries local Property by UUID while live pages use DDF ListingKey, so id types are incompatible.
- [ ] **QA-24** [P1] AI module throws at boot if GEMINI_API_KEY unset — _api/src/modules/ai/ai.service.ts:52_ — getOrThrow in constructor crashes whole API; AiModule always registered. (Currently mitigated by a placeholder key in api/.env added this session.)

### Backend / Infra
- [ ] **QA-25** [P2] Saved/visited resolution N+1 to external DDF — _api/src/modules/users/users.service.ts:140-147_ — one DDF round-trip per unsynced key, uncached.
- [ ] **QA-26** [P2] trackVisited POSTs unauthenticated on every property view — _src/hooks/useTrackVisited.ts:8-13_ — fires for signed-out users → guaranteed 401.

### Other
- [ ] **QA-27** [P2] getProperty/getProperties API clients point at unused/partial routes — _src/lib/api/properties.ts:4-5_ — dead clients hitting UUID path that doesn't match live ListingKeys.

## 📦 Previous Sprints (completed)

### Frontend (FE)
- [x] **FE-2** — Tabbed Facts & features panel
- [x] **FE-307** — Map price pins skip null coords
- [x] **FE-401** — Property detail page
- [x] **FE-402** — Property gallery (hero + thumbs)
- [x] **FE-403** — Property stats bar
- [x] **FE-404** — Neighbourhood context score
- [x] **FE-405** — Mortgage analysis calculator
- [x] **FE-406** — Nearby open houses carousel
- [x] **FE-407** — Market context panel
- [x] **FE-408** — Assessment history table
- [x] **FE-409** — Sales history table
- [x] **FE-410** — Listing activity map
- [x] **FE-411** — Action bar (save/share/contact)
- [x] **FE-412** — Track visited on page load
- [x] **FE-501** — Neighbourhood detail page
- [x] **FE-601** — Dashboard page (protected)

### Backend (BE)
- [x] **BE-303** — Algolia sync on DDF upsert
- [x] **BE-305** — Global Redis search cache
- [x] **BE-306** — Saved searches API
- [x] **BE-401** — Property detail endpoint
- [x] **BE-402** — Nearby open houses endpoint
- [x] **BE-403** — Market context endpoint
- [x] **BE-404** — Assessment history endpoint
- [x] **BE-405** — Sales history endpoint
- [x] **BE-406** — Similar properties endpoint
- [x] **BE-408** — Cached property detail service
- [x] **BE-501** — Neighbourhood detail service
- [x] **BE-502** — Neighbourhood listings
- [x] **BE-503** — Neighbourhood stats
- [x] **BE-504** — Neighbourhood market data
- [x] **BE-505** — Neighbourhood agents
- [x] **BE-601** — Saved properties API
- [x] **BE-602** — Visited properties API
- [x] **BE-604** — User dashboard aggregate

## 🗺️ Neighbourhood Detail Page — design + livability (2026-07-20)
_New neighbourhood detail page from a design session (approved full-page mockup: cinematic split hero + market snapshot + reel → editorial narrative → AI "Why this fits you" card → Local essentials grid → forest-green Livability panel → Local information tiles → live CREA listings → CTA banner). Brand: cream #FAF9F6 · forest #1C3829 · lime #A3E635 · Playfair headings / Inter body. Livability is a deterministic composite (Walk 0.30 · Schools 0.25 · Amenities 0.25 · Transit 0.20) → 0–100 + "Top X% in <region>" percentile; **healthcare intentionally excluded** from the blend (proximity ≠ access) and kept as a context card only. Schools are **locations-only** (no CA ratings available). Data: OSM/Overpass POIs, agency GTFS, provincial school data, Google Maps Platform (proxied+cached), CREA DDF feed. See [[project_data_pipeline]]. Backend #NBHD-01..09 · Frontend #NBHD-10..16 (can start in parallel on mocked API) · Docs #NBHD-17. Sizes: S ≤0.5d · M 1–2d · L 3–5d · XL >5d._

- [x] **NBHD-01** [P1] _(Foundation)_ **Define neighbourhood boundaries + reference set** — establish the geographic unit (StatsCan census tracts / dissemination areas, or curated named-neighbourhood polygons) and the percentile reference pool; store polygons + centroids in PostGIS. Foundational — POI radius queries, walkability sampling, and the "Top X%" label all depend on it. Document the reference frame (city vs metro vs province). `M` · blocks: NBHD-02,04,05,06,07 — _done: `boundary`(GeoJSON Json, no PostGIS — DB lacks the extension)/`centroidLat`/`centroidLng`/`referenceRegion` on `Neighbourhood`; `NeighbourhoodBoundary` type; migration `add_neighbourhood_boundaries`. Polygon ingestion is a separate data task; `referenceRegion` defaults to `city ?? province`._
- [x] **NBHD-02** [P1] _(Data)_ **POI ingestion pipeline (OSM/Overpass)** — tiled Overpass pull mapped into categories (grocery, restaurants, coffee, shopping, schools, parks, banks, entertainment, errands); cache raw POIs with an `osm_snapshot_version`, refresh quarterly; ODbL attribution. Feeds walkability, amenities, shop & eat, schools, nature & parks. Decide OSM-only vs Google Places hybrid. `M` · deps: NBHD-01 — _done: `PoiIngestionService` (Overpass POST, category mapping, `snapshotVersion` YYYY-Qn, `ODBL_ATTRIBUTION`, `ingestPoisForNeighbourhood`/`ingestAllNeighbourhoods`); `NeighbourhoodPoi` model + migration `add_neighbourhood_poi`. OSM-only for v1. Batch is a method, not scheduled._
- [x] **NBHD-03** [P1] _(Data)_ **Google Maps proxy + caching layer** — server-side NestJS proxy for Static Maps, Street View Static, Places Nearby; key stays server-side, rate-limited, cached per-neighbourhood with TTL (respect Places caching ToS). Powers Local Information tiles. Prefer Static images over live map instances for tiles. `M` — _done: `GoogleMapsProxyService` (Static Maps/Street View URL builders, Places Nearby cached in Redis 24h). Key resolves `GOOGLE_MAPS_API_KEY` → falls back to existing `GOOGLE_PLACES_API_KEY`; placeholder URLs when neither is set._
- [x] **NBHD-04** [P1] _(Scoring)_ **Walkability scoring batch job** — Python (osmnx + pandana): grid-sampled nearest-N POIs by network walking distance, distance-decay (full ≤400m → 0 at 2400m), diminishing-returns depreciation, category weights, × pedestrian-friendliness (intersection density + block length). Calibrate 0–100 vs fixed anchors; sanity-check against real Walk Score. Neighbourhood = median of grid points. v1 shortcut: straight-line ×1.4 detour. Writes to Postgres. **Decision (2026-07-20): build from OSM — no paid Walk Score API.** `L` · deps: NBHD-01,02 — _done: `WalkabilityService` JS v1 (straight-line ×1.4 detour, decay 400→2400m, log diminishing returns, category weights, fixed-anchor 0–100). osmnx/pandana network distance + pedestrian-friendliness deferred to a later release._
- [x] **NBHD-05** [P1] _(Scoring)_ **Transit scoring from GTFS** — 0–100 sub-score from agency GTFS (BC Transit/TransLink): per-stop route value weighted by peak trips/day + distance-decay, diminishing returns, normalized. **Decision (2026-07-20): build from agency GTFS — no paid Transit Score API.** `M` · deps: NBHD-01 — _done: `TransitService` reads `TRANSLINK_GTFS_PATH`/`BC_TRANSIT_GTFS_PATH`; no feed wired in env so returns `{ score: null, source: 'unavailable' }` (livability redistributes its weight). GTFS parse is TODO-stubbed._
- [x] **NBHD-06** [P1] _(Scoring)_ **Schools-access + amenities sub-scores** — schools (locations-only): mean of distance-decayed nearest school per level (elem/middle/secondary), rewards full-level coverage. Amenities: distance-decayed POI density with per-category caps (variety not raw count), no double-count with walkability. Both 0–100, stored. `M` · deps: NBHD-02 — _done: `SchoolsAmenitiesService` (schools decay 800→3000m, all-levels bonus, v1 level inference from name; amenities per-category cap 3 with distinct normalization). Persisted via NBHD-07._
- [x] **NBHD-07** [P1] _(Scoring)_ **Livability blend + percentile + versioned storage** — weighted arithmetic mean (Walk 0.30 · Schools 0.25 · Amenities 0.25 · Transit 0.20) → 0–100; percentile rank across the reference set → "Top X% in <region>". Persist `{subscores, weights_version, livability, percentile, computed_at}` for reproducibility. Healthcare excluded. Geometric-mean A/B variant later. `M` · deps: NBHD-04,05,06 — _done: `LivabilityService.computeAndSaveScores`/`computeAllScores` (null-transit weight redistribution, within-`referenceRegion` percentile). Persists `walkability/schools/amenities/transitSubScore/livabilityScore/livabilityPercentile/livabilityWeightsVersion('v1')/livabilityComputedAt`; migration `add_neighbourhood_scores`. **Note:** used new `transitSubScore Float` — legacy seeded `transitScore Int` left intact._
- [x] **NBHD-08** [P1] _(AI)_ **AI personalization — priorities + match score (deterministic v1)** — recompute livability with the user's personalized weights → match %; inputs = stated priorities (edit-priorities UI) + saved searches/listings + revealed preference (PostHog + owned events). Transparent reason chips + caution chips; templated sentence for v1 (LLM synthesis = AI release). Define cold-start / no-data state. `L` · deps: NBHD-07 — _done: `PersonalizationService` maps `onboardingData.lifestylePriorities` → weights (rank-ordered, normalized), recomputes match %, emits templated reason/caution chips; cold-start returns default weights + `isPersonalized:false`. Saved-search/revealed-preference inputs deferred (only stated priorities in v1)._
- [x] **NBHD-09** [P1] _(API)_ **Neighbourhood detail aggregate endpoint** — one endpoint serving market snapshot (median price + trend + DOM from CREA), livability + breakdown + percentile, local essentials, local-information tile data (cached Google assets), live listings (CREA), and the personalized match block for signed-in users. Reads precomputed values; no heavy compute in the request path. `M` · deps: NBHD-03,07,08 — _done: `GET /neighbourhoods/:slug/detail` (new `OptionalClerkAuthGuard` — anonymous + signed-in). Base block cached 30min, per-user personalization 10min. `priceChange30d` is `null` (no price-history table yet)._
- [x] **NBHD-10** [P1] _(FE)_ **Cinematic hero + market snapshot + reel card** — split hero (photo panel with name on a dark scrim) + Market snapshot grid (Median price + % trend, Walk score, Transit, Days on market) + Neighbourhood reel card (v1 fallback: Street View flythrough / gallery). Brand tokens; responsive stacking. `M`
- [x] **NBHD-11** [P1] _(FE)_ **AI "Why this fits you" card + cold-start** — forest-bordered card: sparkle icon, match % badge, personalized sentence, positive + caution reason chips, priority pills, "Edit priorities" control. Cold-start variant for no-data users. Editorial narrative sits above. `M` · deps: NBHD-08 (data)
- [x] **NBHD-12** [P1] _(FE)_ **Local essentials card grid** — four responsive cards: Schools (nearest by level + distances), Healthcare (hospital + clinics, context only), Nature & parks (forest highlight card + lime "Explore atlas"), Shop & eat (category chips + walkable radius). `M`
- [x] **NBHD-13** [P1] _(FE)_ **Livability score panel** — forest-green panel: big lime headline score, "Top X% in <region>", four labelled horizontal bars (Walkability, Schools access, Amenities, Transit) with lime fills. "How we calculate this" link → methodology. `S`
- [x] **NBHD-14** [P2] _(FE)_ **Local information tiles** — four tiles (Map view, Street view, Schools, Shop & eat), each a Static thumbnail via the Google proxy + label/count; click opens the interactive experience (Mapbox / Street View embed / filtered lists). `S` · deps: NBHD-03
- [x] **NBHD-15** [P1] _(FE)_ **Live listings row + CTA banner** — horizontal-scroll CREA cards (price, address, bd/ba, "Data provided by CREA" + REALTOR® attribution) with a "For you" lime flag on matches + "See all"; forest CTA banner ("Make <Neighbourhood> your home?") with Explore listings / Connect with expert. Ensure CREA/REALTOR.ca attribution compliance. `M`
- [x] **NBHD-16** [P1] _(FE)_ **Assemble page + routing + responsive pass** — compose all sections into the neighbourhood detail route, wire the aggregate API, add loading/empty/error states, full mobile pass (hero stacking, grids collapsing, listings scroll). Verify in browser preview vs the approved design. `M` · deps: NBHD-09..15
- [x] **NBHD-17** [P2] _(Docs)_ **Livability + walkability methodology docs** — `docs/livability-methodology.md` + `docs/walkability-methodology.md`: sub-indices, data sources (OSM/Overpass, GTFS, provincial schools, CREA), distance-decay + category weights + depreciation, pedestrian-friendliness, 0–100 calibration, blend weights, percentile ranking, versioning, healthcare-excluded decision. Backs the "How we calculate this" UI link. `S`
- [ ] **NBHD-18** [P0] _(Data)_ **Municipal open-data boundaries → centroids + polygons** — ingest official neighbourhood/local-area boundaries from municipal open data portals (Vancouver's 22 local areas verified working; survey Burnaby, Surrey, Richmond, New Westminster, North/West Vancouver, Coquitlam, Port Moody, Port Coquitlam), store GeoJSON in the unused `Neighbourhood.boundary` column and derive centroids from polygon geometry. Replaces point geocoding as the source of truth: a polygon cannot collapse onto a city centre or land in the wrong municipality. Fixes the residual collapse in Port Moody (3 distinct scores / 7) and Port Coquitlam (3/7), the Granville row geocoded into Vancouver instead of Richmond, and the two unrated rows (Sullivan Heights, Mary Hill). Unlocks polygon-scoped POI queries in place of the 1500m radius approximation. Match on name with a containment check; leave centroid null rather than accept an unverified match. `M` · blocks: launch
- [x] **NBHD-19** [P1] _(Scoring)_ **Implement the GTFS transit parser (finish NBHD-05)** — NBHD-05 shipped as a stub: the env plumbing (`TRANSLINK_GTFS_PATH`/`BC_TRANSIT_GTFS_PATH`) and the `TransitService.parseGtfs` signature exist, but parsing is unimplemented, so `transitSubScore` is `null` for every neighbourhood and the livability blend runs on 3 dimensions (transit's 0.20 redistributed). Verified this session: TransLink publishes a **free, no-auth static GTFS feed** at `https://gtfs-static.translink.ca/gtfs/google_transit.zip` (16 MB, HTTP 200) covering **all 10 launch cities** (Metro Vancouver = TransLink; no BC Transit needed for launch). Feed contains `stops.txt` (8,904 stops) and `stop_times.txt` (**94 MB — must be streamed, not read whole**), plus `trips.txt`/`routes.txt`/`calendar.txt` for peak weighting. Implement: unzip + stream stop_times → trips/day per stop (peak-hour weighted) → per-neighbourhood distance-decayed sum from the centroid, diminishing returns, normalize 0–100 (calibrate against fixed anchors like NBHD-04). Download the feed into `api/data/`, refresh quarterly like the OSM snapshot. Then re-run scoring so the Transit bar shows real numbers and livability uses the full 4-dimension blend. Document the calibration in `docs/livability-methodology.md` (§ Transit). **Scope: the 122 launch-set neighbourhoods only (Metro Vancouver) — TransLink's feed doesn't cover the rest of BC, so a neighbourhood with no TransLink stop in range stays `transit: null` rather than being scored 0.** `M` · deps: NBHD-05,07 · branch: `feat/nbhd-transit-gtfs`

### 🎨 Design-alignment pass — match the approved "Admirals" mockup (2026-07-20)
_The first FE pass (NBHD-10..16) was built against a simplified brief and diverges from the approved full-page mockup. These tasks realign each section to the design (split hero with embedded market + reel, "Why this fits you" AI card, 4-card essentials grid, big-number livability, four compact info tiles, "For you" listing flags). Reference: the approved Admirals mockup. Files under `src/components/neighbourhood/`._

- [x] **NBHD-D01** [P1] _(FE)_ **Split hero card (replace full-bleed hero)** — rebuild `NeighbourhoodHero` as a two-panel card: LEFT photo panel (`city · province` pill + name on a dark scrim, rounded); RIGHT white panel holding the market snapshot + the reel card. Stack panels on mobile. Supersedes the current full-bleed hero + separate market strip. `M`
- [x] **NBHD-D02** [P1] _(FE)_ **Market snapshot — correct stat set, embed in hero** — stats per design: Median Price (+ % trend arrow), Walk Score, Transit, Days on Market (drop "Active Listings" / standalone strip). Render inside the hero's right panel (`MarketSnapshot` becomes a sub-component of the hero). `S` · rel: NBHD-D01
- [x] **NBHD-D03** [P1] _(FE)_ **Neighbourhood reel card** — play-button card in the hero right panel ("Neighbourhood reel · A 2-minute cinematic tour"); v1 fallback opens a Street View flythrough / photo-gallery modal. `S` · rel: NBHD-D01
- [x] **NBHD-D04** [P1] _(FE)_ **Editorial narrative block** — prose intro (large serif body, 2–3 sentences from `neighbourhood.description`) directly below the hero, above the AI card. New component `NeighbourhoodNarrative`. `S`
- [x] **NBHD-D05** [P1] _(FE)_ **"Why {name} fits you" AI card** — forest-bordered card: sparkle icon, "X% MATCH" lime badge, personalized sentence with bolded phrases, green reason chips + amber caution chip, priority pills, "Edit priorities" control; cold-start variant for no-data users. New component `WhyItFitsCard`, consumes `personalization`. This is the real NBHD-11 that the first pass skipped (livability took its slot). `M` · deps: NBHD-08 (data)
- [x] **NBHD-D06** [P1] _(FE)_ **Local essentials — 4-card grid (replace tabs)** — rebuild `LocalEssentials` as a responsive card grid, not tabs: Schools (nearest by level + km), Healthcare (hospital + clinic + context line), Nature & parks (DARK forest card, "{n} green spaces within 1 km" + lime "Explore atlas →"), Shop & eat (category count chips + "Walkable · {r} m radius"). Eyebrow "THE INTELLIGENCE REPORT" + right-aligned "{name} · {city}". `M`
- [x] **NBHD-D07** [P1] _(FE)_ **Livability panel — big-number layout** — replace the SVG ring with a big lime score number + "Livability" + "Top X% in {city}" on the left; bars on the right (Walkability, Schools access, Amenities, Transit) with values. Rename "Schools"→"Schools access". Add "How we calculate this" → methodology link. Keep reduced-motion animation. `S`
- [x] **NBHD-D08** [P2] _(FE)_ **Local information — four compact tiles** — rebuild `LocalInfoTiles` as four small icon tiles (Map view · Street view · Schools "{n} nearby" · Shop & eat "{n} places") with icon + label + sublabel/count; click opens the interactive experience. Replaces the two large 240px image tiles. Eyebrow "EXPLORE THE AREA". `S` · deps: NBHD-03
- [x] **NBHD-D09** [P1] _(FE)_ **Live listings — For-you flag + REALTOR® badge + header See-all** — add a lime "✨ For you" flag on personalized matches, a "REALTOR®" badge on the image top-left, and move "See all →" into the section header. Keep CREA/REALTOR.ca attribution. `S`
- [x] **NBHD-D10** [P1] _(FE)_ **Reassemble page + wire new fields + responsive pass** — section order per mockup: split hero → editorial narrative → AI fit card → essentials grid → livability → info tiles → live listings → CTA. Extend `NeighbourhoodDetailResponse` + composer with the fields the design needs (price trend, walkScore/DOM in snapshot, reel URL, park/shop counts, per-tile counts, personalization chips). Full mobile pass; verify in browser vs the mockup. `M` · deps: NBHD-D01..D09

## 🧭 NBHDCTA — Neighbourhood detail page CTA cleanup (2026-07-30)
_Quick fixes to the neighbourhood detail hero + bottom CTA banner, called out by RC from a live screenshot pass._

- [x] **NBHDCTA-01** [P2] _(Bug)_ **Hide the "Neighbourhood reel" button** — no reel content exists yet (`ReelCard` is a static play-button card with no video wired up). Remove/hide it from the hero's right panel. `S` · _src/components/neighbourhood/NeighbourhoodHero.tsx:79_ — **done: `ReelCard` deleted, hero right panel now just the market snapshot. Browser-verified 2026-07-30 on Ambleside.**
- [x] **NBHDCTA-02** [P1] _(Bug)_ **"Explore Listings" should actually filter to the neighbourhood** — the button already links to `/search?neighbourhood=${slug}` (`NeighbourhoodCTA.tsx:19`), but the search page **ignores the `neighbourhood` query param entirely** — no handling found in `src/app/(main)/search`. Wire it up: read the param on load, resolve it to a filter (name/city or a bounding query), and apply it to the search results so clicking through actually scopes listings to that neighbourhood. `M` · _src/components/neighbourhood/NeighbourhoodCTA.tsx:19, src/app/(main)/search/_ — **done: CTA now sends `lat`/`lng` (+ `name`) alongside `neighbourhood`; `search/page.tsx` boxes a 3km bbox around the centroid (matches NBHDCTA-04's radius) and passes it through as a "locked" bbox that both the Feed (`city`→`bbox` swap) and Map/pins queries honor ahead of the geolocation/Vancouver default, until the user pans the map. Results list label shows the neighbourhood name instead of the fallback city. Caught and fixed a `toNum()` bug along the way — it rejected negative numbers, silently dropping every `lng` (always negative in Canada); added a signed `toSignedNum()` for lat/lng. Browser-verified 2026-07-30: Ambleside (West Vancouver) CTA → search returns a `bbox`-scoped feed/map-pins query (no more `city=Vancouver` fallback), results are West Van addresses.**
- [x] **NBHDCTA-03** [P2] _(Bug)_ **Remove "Connect with neighbourhood expert" button for now** — no expert-connect flow exists behind it yet (it currently just links to `/sign-up?intent=tour&neighbourhood=${slug}`, not a real "connect with expert" flow). Remove the button from the CTA banner until that flow is built; keep "Explore Listings" centered/solo. `S` · _src/components/neighbourhood/NeighbourhoodCTA.tsx:24-29_ — **done, browser-verified 2026-07-30.**
- [x] **NBHDCTA-04** [P1] _(Bug)_ **Live listings carousel shows properties outside the neighbourhood** — root cause: `buildLocationClauses()` scopes the "Live listings" query (`getDetailListings`) to an **OR of (a) case-insensitive city-name match, or (b) an 8km lat/lng box around the neighbourhood centroid** — for a big city (e.g. "Vancouver") the city clause alone pulls in listings from anywhere in the whole city, not the specific neighbourhood, and the 8km box is coarse enough to cross into adjacent neighbourhoods too. This is the same coarse-matching gap `NBHD-18` already flagged for POI queries. `M` · deps: NBHD-18 · _api/src/modules/neighbourhoods/neighbourhoods.service.ts:550 (getDetailListings), :938 (buildLocationClauses)_ — **done (interim fix, full fix still deps on NBHD-18's boundary polygons): `buildLocationClauses` now prefers the lat/lng box alone whenever a centroid exists (dropping the citywide OR that was the main leak), city-only match is now the no-centroid fallback only; radius tightened 8km → 3km, matching the radius the FE now uses for NBHDCTA-02's search-page bbox so both surfaces agree on "how wide a neighbourhood is." tsc clean (FE + API).**

## Summary
- **Neighbourhood Detail Page (design + livability):** 17 tasks (NBHD-01..17) — Backend/scoring 9 · Frontend 7 · Docs 1 · **+10 design-alignment (NBHD-D01..D10, FE)** to match the approved mockup
- **Co-Founder Review (QA verification pass):** 13 tasks — 5 P0 · 6 P1 · 2 P2 (REV-01..REV-13)
- **Data Pipeline v1 (deterministic):** 25 tasks (DATA-01..DATA-25) — ~33.5 eng-days
- **AI / Learned Reranker v2 (next release):** 5 tasks (AI-01..AI-05) — **AI effort ≈ 15–25 eng-days (XL)**
- **Backlog (QA Audit):** 27 tasks — 2 P0 · 14 P1 · 11 P2
- **Completed this session:** 3 (FIX-01..FIX-03)
- **Previous-sprint tickets shipped:** 33 (16 FE · 17 BE)
