// Closes the overlay when navigating client-side back to "/" (the index route
// isn't covered by the [...catchAll] slot below, which needs ≥1 segment).
export default function PropertyModalIndex() {
  return null
}
