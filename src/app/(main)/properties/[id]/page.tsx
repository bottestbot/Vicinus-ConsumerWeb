export default function PropertyDetailPage({ params }: { params: { id: string } }) {
  return <div className="pt-16 min-h-screen bg-black text-white p-8">Property {params.id} — coming in Sprint 4</div>
}
